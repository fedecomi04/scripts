# ZED real-camera readiness audit (salvaged from stopped workflow wf_61420a1f-280)

**Status:** 6 audit areas + coverage critic completed. Adversarial verification phase did NOT run (workflow stopped) — findings below are UNVERIFIED auditor claims; treat blockers/required as high-confidence, spot-check the rest.

## AREA: reader/spawn (live_shm_reader.py)

- **[BLOCKER]** `dynamic_gs/utils/live_shm_reader.py:154-214 (constants 154-159; checks 177-186; bash wrapper 210-214)` — _spawn_publisher hardwires the sim-ROS publisher: ROS_PUBLISHER_CONDA_ENV="dynamic_gs_ros" (L154), ROS_SETUP_SCRIPT="/opt/ros/noetic/setup.bash" (L155), _PUBLISHER_SCRIPT=live_ros_publisher.py (L157), hardcoded miniconda interpreter path (L158-159). It raises RuntimeError if /opt/ros/noetic/setup.bash is missing (L182-186) — so on a ROS-less real-camera machine the reader cannot even start — and the spawn command sources ROS + exports PYTHONNOUSERSITE=1 in a bash -c wrapper (L211-214).
    - CHANGE: Add a backend switch inside _spawn_publisher (env var e.g. DGS_CAMERA_BACKEND=zed, or a constructor arg threaded from LiveDynamicGSPipelineConfig at dynamic_gs_pipeline_live.py:135-139): for the zed backend select a new live_zed_publisher.py script + the pyzed env's python, skip the ROS setup.bash sourcing and its existence check, and exec the interpreter directly. ~30 LOC change in this one function; everything from the Popen call down (L238-249) stays identical. The new publisher is 1 new module (publisher-side) that must accept the exact same CLI contract the reader passes: --live-root, --shm-name, --keyframe-translation-m, --keyframe-rotation-deg, --wipe-live-root, --record-replay (L188-201).
- **[REQUIRED]** `dynamic_gs/utils/live_shm_reader.py:547-559 (callers: dynamic_gs/utils/live_session.py:107-145, 705, 946)` — pause_gazebo_physics / unpause_gazebo_physics send the sim-only "pause_gazebo"/"unpause_gazebo" control ops (pause before the SAM3 anchor grab, unpause after). With a real camera there is no physics to pause. The reader already tolerates failure (try/except → False), BUT only after _read_response's default 600 s timeout (L446) if the publisher never replies — an unknown-op that goes unanswered hangs the session ~10 minutes.
    - CHANGE: Two-part: (a) the new ZED publisher's command loop must reply promptly with valid JSON ({"ok": false} or a no-op {"ok": true}) to pause_gazebo/unpause_gazebo and to ANY unknown op; (b) optionally short-circuit these two reader methods when backend==zed (return False without sending) — ~6 LOC. The live_session call sites need no change since they already branch on the bool.
- **[REQUIRED]** `dynamic_gs/utils/live_shm_reader.py:446-483` — Not sim-specific in itself but a hard constraint the sim publisher silently satisfies: _read_response treats EVERY stdout byte as protocol — any non-JSON line raises RuntimeError (L474-478) and EOF raises (L461-464). The ZED SDK by default can print init/log lines to the console; if the new publisher (or pyzed) writes anything to stdout besides the ready event + JSON responses, the reader dies at startup.
    - CHANGE: In the new ZED publisher: open the camera with sdk_verbose disabled (or redirect SDK logging to a file/stderr), and route all diagnostic prints to stderr (the reader already captures stderr to /tmp/dgs_live_publisher/publisher.stderr.log, L229-245). No reader change.
- **[REQUIRED]** `dynamic_gs/utils/live_shm_reader.py:398-401, 138 (consumer: dynamic_gs/dynamic_gs_pipeline_live.py:568)` — The reader performs zero depth validation — peek_latest memcpys the float32 depth buffer verbatim. Sim depth was clean; ZED depth contains NaN/Inf/0 invalid pixels which would flow straight into torch tensors (pipeline_live.py:568 torch.from_numpy(frame.depth_m)) and poison fusion/back-projection.
    - CHANGE: Per the byte-identical-SHM plan, sanitize in the NEW ZED publisher before the SHM write: np.nan_to_num(depth, nan=0.0, posinf=0.0, neginf=0.0) and clamp below min-range to 0.0, establishing 0 = invalid as the contract. The reader stays untouched. Downstream consumers of depth_m==0 must be audited by the agents covering fusion/tracking (outside this file).
- **[REQUIRED]** `dynamic_gs/utils/live_shm_reader.py:605-624` — cameras_from_live_frame builds a CameraType.PERSPECTIVE Cameras with fx/fy/cx/cy only — no distortion parameters exist anywhere in the SHM header (L57, L69) or ready handshake (L296-303). The sim camera was an ideal pinhole; a real ZED lens is not.
    - CHANGE: No reader change IF the ZED publisher publishes the RECTIFIED left image (ZED SDK default retrieve_image(VIEW.LEFT) is rectified) together with the rectified-left calibration (calibration_parameters.left_cam fx/fy/cx/cy). Document this as part of the publisher contract; do NOT use raw/unrectified views.
- **[RECOMMENDED]** `dynamic_gs/utils/live_shm_reader.py:223-226` — The spawner scrubs LD_LIBRARY_PATH / CPATH / LIBRARY_PATH / CUDA_HOME from the child env (correct for the ROS env, whose C-extensions break under dynamic_gs's conda libstdc++). The ZED SDK (pyzed → libsl_zed.so) requires CUDA at load time; it normally resolves via system ldconfig, but if the ZED env needs any of these vars the blanket scrub will break it.
    - CHANGE: Make the env-var scrub list backend-conditional inside the same backend switch (keep the scrub for ros backend; for zed, verify pyzed imports with the scrubbed env and only re-add vars if the import fails). A few LOC, decided when the ZED env is built.
- **[RECOMMENDED]** `dynamic_gs/utils/live_shm_reader.py:271-272 (forwarded to publisher at 192-193; defaults duplicated at dynamic_gs_pipeline_live.py:67,71)` — The 2 cm / 20° keyframe dedup thresholds are tuned on smooth robot-arm sweeps. The reader only FORWARDS them as CLI args — the actual dedup runs publisher-side via an inlined filter (live_ros_publisher.py:305, 548, 951), NOT via keyframe_filter.py. Handheld ZED motion (tremor near thresholds, VIO jitter) may yield a different keyframe density than the arm did.
    - CHANGE: The new ZED publisher must inline/import an equivalent SE(3) dedup filter (DynamicKeyframeFilter in keyframe_filter.py is directly reusable — pure numpy/torch, no ROS). Thresholds likely fine to start but should be re-validated on a handheld capture; expose them exactly as the existing CLI args so no reader change is needed.
- **[RECOMMENDED]** `dynamic_gs/utils/live_shm_reader.py:373-414 (copy at 398-401)` — Resolution scaling, not correctness: peek_latest copies rgb+depth+mask out of SHM every call. At sim 800x800 that is ~5.1 MB/peek; at ZED 1920x1200 it becomes ~18.4 MB/peek (6.9 rgb + 9.2 depth float32 + 2.3 mask). At the 17-30 Hz tracker tick rate that is ~310-550 MB/s of host memcpy, plus larger downstream XFeat/render inputs.
    - CHANGE: No code change in the reader (it is fully H/W-generic). Prefer the 960x600 ZED mode initially, or measure peek_latest + per-tick cost at 1920x1200 before committing (per the no-timing-guesses rule).
- **[OPTIONAL]** `dynamic_gs/utils/live_shm_reader.py:1-19, 423-429` — Stale ROS wording: module docstring says "live ROS publisher" and names the deleted env radiance_ros_4060 (L5); wait_for_first_frame's timeout message says "check that ... ROS topics are publishing" (L427-429); comment at L423-424 mentions ROS callbacks. Misleading diagnostics on a ZED run.
    - CHANGE: Reword to backend-neutral text ("publisher subprocess is running and producing frames") when the backend switch lands. Pure text, ~6 lines.
- **[OPTIONAL]** `dynamic_gs/utils/live_shm_reader.py:47-50` — LIVE_ROOT defaults to a hardcoded absolute path under data_teleoperation/datasets/live. Machine-specific rather than sim-specific; already overridable via DGS_LIVE_ROOT (set by bootstrap_live.sh).
    - CHANGE: None strictly; keep using DGS_LIVE_ROOT. Optionally derive the default from the repo root.

  _notes:_ VERDICT ON THE WORKING PLAN: keeping the SHM contract + reader untouched is feasible — nothing in the SHM layout, seqlock, frame dtypes, or control transport is ROS- or sim-bound. The only reader-side code that MUST change is _spawn_publisher (one function, ~90 LOC today; ~30 LOC backend switch), which is also the natural plug-in point for a sim-ROS vs ZED selector. Scale of the publisher side: 1 new module (live_zed_publisher.py); the existing live_ros_publisher.py is ~1450+ LOC, but a large share is SHM writer + control-command loop + recording/keyframe logic that is camera-agnostic and could be factored into a shared base to avoid duplication — that refactor decision belongs to whoever writes the new publisher. CONTRACT SPEC THE NEW PUBLISHER MUST HONOR (from the reader's perspective): (1) same CLI args incl. --record-replay; (2) first stdout line = {\"event\":\"ready\", width, height, fx, fy, cx, cy, num_slots, slot_bytes, header_bytes}; (3) stdout carries ONLY JSON lines (ZED SDK verbosity must not leak to stdout — reader hard-fails on non-JSON); (4) prompt JSON reply to every op including pause_gazebo/unpause_gazebo and unknown ops (otherwise 600 s hang at live_shm_reader.py:446); (5) SHM header byte-identical to _HDR_FMT with magic DGS\\0; (6) BGR uint8 (strip ZED alpha), float32-metre depth with NaN/Inf/sub-min-range mapped to 0.0, all-255 mask, OpenGL c2w float64 from RIGHT_HANDED_Y_UP+METER tracking of the rectified LEFT sensor, with the rectified-left intrinsics. OPEN QUESTIONS / RISKS OUTSIDE MY FILES (flagging for other agents): (a) downstream treatment of depth==0 as invalid (fusion ICP, XFeat depth filter, back-projection) needs auditing — sim never produced invalid pixels; (b) ZED VIO drift: the dedup math is fine, but OnlineFusion's per-frame ICP and the static-pose ICP rewrite (invariant #3) were validated against sub-mm FK+ICP poses, not drifting VIO; (c) Gazebo sky background (0.86,0.92,1.0) baked into both models (invariant #6) is model-side, not reader-side; (d) live_session.py's pause/unpause + atexit unpause hook (live_session.py:96-145) and its SAM3 anchor flow assume a pausable world — semantics become \"operator holds the scene still\"; (e) timing: peek_latest copy grows 3.6x at 1920x1200 — measure, don't assume (per the no-timing-guesses rule). Also note the duplicated default keyframe thresholds (reader ctor 271-272 vs pipeline config 67/71) — keep them in sync if retuned for handheld capture.

  **Reusable (works as-is):**
  - The reader imports no ROS anywhere — full import list is atexit/json/os/struct/subprocess/sys/threading/time, dataclasses, multiprocessing.shared_memory, pathlib, typing, numpy, torch, nerfstudio.cameras. The 'reader never imports rospy' claim is verified.  (dynamic_gs/utils/live_shm_reader.py:22-39)
  - The SHM contract is ROS-free and a new ZED publisher can reproduce it byte-identically: header struct _HDR_FMT with magic b"DGS\0", H/W, fx/fy/cx/cy, latest_seq, per-field offsets, ready/shutdown flags; seqlock-style lock-free read (read latest_seq, copy slot, re-verify seq) is publisher-agnostic.  (dynamic_gs/utils/live_shm_reader.py:52-106 (format + offsets), 320-328 (magic check + offset adoption), 373-414 (seqlock peek))
  - The reader is fully resolution- and aspect-ratio-agnostic: H/W/intrinsics come from the publisher's ready message and slot views are built generically from them — no 800x800, square, or aspect assumption exists. 1920x1200 or 960x600 (16:10) work unchanged.  (dynamic_gs/utils/live_shm_reader.py:296-306 (ready adoption), 345-369 (_build_slot_views from H,W))
  - Pose handling is pass-through with no convention conversion: the float64 4x4 from SHM is handed raw to nerfstudio Cameras as camera_to_worlds, which expects OpenGL c2w — exactly what ZED positional tracking yields under COORDINATE_SYSTEM.RIGHT_HANDED_Y_UP (with UNIT.METER). Zero reader changes for the pose source swap.  (dynamic_gs/utils/live_shm_reader.py:352-353 (float64 16-value pose view), 605-624 (cameras_from_live_frame uses c2w[:3,:4] directly))
  - Depth contract already matches ZED's native output: float32 metres in SHM (LiveFrame.depth_m), no unit conversion in the reader. (Sanitization of NaN/Inf is the publisher's job — see sim_specific.)  (dynamic_gs/utils/live_shm_reader.py:127-139 (LiveFrame docstring: depth_m float32 metres), 361-363 (float32 slot view))
  - Mask semantics survive the no-robot case with zero changes: the reader passes the uint8 mask through verbatim, and consumers binarize with keep = nonzero — an all-white (255) mask trivially satisfies this. No gripper/URDF logic exists in the reader.  (dynamic_gs/utils/live_shm_reader.py:364-366, 400, 409; consumers: dynamic_gs/utils/live_session.py:183 (mask_keep > 0), dynamic_gs/dynamic_gs_pipeline_live.py:569 (.bool()))
  - RGB contract (uint8 HxWx3 BGR) is consumer-enforced and ZED-friendly: ZED returns BGRA, so the publisher only strips alpha; downstream already converts BGR->RGB itself.  (dynamic_gs/utils/live_shm_reader.py:137, 358-360; consumers dynamic_gs/dynamic_gs_pipeline_live.py:566 and dynamic_gs/utils/live_session.py:182 do the BGR->RGB flip)
  - The control-pipe transport (JSON-lines over stdin/stdout, with stderr tee'd to /tmp/dgs_live_publisher/) is ROS-free and reusable as-is; the ops actually exercised by runtime callers are capture_anchor, start_recording, stop_recording, num_recorded, build_init_pcd (fallback path only), shutdown — all camera-agnostic. The two anchor-save ops (save_anchor_for_sam3 L520, save_anchor_intrinsics_and_depth L529) are currently UNCALLED by any caller (live_session uses its own local _save_anchor_for_sam3/_save_anchor_intrinsics_and_depth that read the LiveFrame directly), so the ZED publisher may stub them.  (dynamic_gs/utils/live_shm_reader.py:433-483 (transport), 485-545 (ops); callers dynamic_gs/utils/live_session.py:464-465,606,836-837,909,926 and scripts/capture_only.py:210-217; local saves at live_session.py:175,200)
  - keyframe_filter.py (DynamicKeyframeFilter) contains nothing sim-specific: pure SE(3) math — Euclidean translation on the c2w column + SO(3) geodesic arccos(0.5(trace-1)) — accepting numpy or torch, units = metres/radians. Works identically on ZED VIO poses. Note it is NOT used by the reader: its consumers are the datamanager/pipeline (static keyframe filter + recorded dynamic dedup); the live-capture dedup runs inside the publisher via an inlined copy.  (dynamic_gs/utils/keyframe_filter.py:50-84 (the math); consumers dynamic_gs/dynamic_gs_datamanager.py:125, dynamic_gs/utils/__init__.py:22; publisher inline at dynamic_gs/utils/live_ros_publisher.py:305,548)
  - The stamp handling is wall-clock-safe: the reader stores stamp_sec opaquely (never compares it to time.time()), and the downstream stamp gate is monotonic-with-reset-rearm, which is strictly simpler with a real camera's monotonically increasing timestamps than with sim time.  (dynamic_gs/utils/live_shm_reader.py:356-357, 402, 407-410; gate at dynamic_gs/dynamic_gs_pipeline_live.py:249-262)
  - Reader lifecycle (singleton, ready handshake with 30 s timeout, resource_tracker unregister, atexit/close, graceful shutdown op then terminate) has no sim dependency and survives a publisher swap unchanged.  (dynamic_gs/utils/live_shm_reader.py:267-331 (init/handshake/attach), 314-319 (resource_tracker), 563-597 (close/atexit))
  - The reader's constructor surface already carries everything a ZED backend needs configured (shm name, keyframe thresholds, wipe flag) and is fed from LiveDynamicGSPipelineConfig — no new plumbing required for the swap beyond the backend selector itself.  (dynamic_gs/utils/live_shm_reader.py:267-284; dynamic_gs/dynamic_gs_pipeline_live.py:63-75, 135-139)

## AREA: publisher (live_ros_publisher.py)

- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:154-186, 211-214, 223-226` — The reader (constructed by live_session.py:445/427 and capture_only.py:169) hardcodes spawning live_ros_publisher.py: _PUBLISHER_SCRIPT points at the ROS publisher (line 157), _spawn_publisher raises RuntimeError if the dynamic_gs_ros env python (177-181) or /opt/ros/noetic/setup.bash (182-186) is missing, and wraps the launch in `bash -c "source /opt/ros/noetic/setup.bash && ..."` (211-214). It also strips LD_LIBRARY_PATH/CUDA_HOME from the child env (223-226), which the pyzed/ZED-SDK process may need for CUDA. This makes the 'keep the reader untouched' plan impossible as stated.
    - CHANGE: Add a publisher-selection switch in _spawn_publisher (e.g. DGS_PUBLISHER=zed env var choosing script path + interpreter + no ROS sourcing; ~15-25 LOC). Everything else in the reader (SHM attach, header decode, peek_latest, control-pipe client) genuinely works unchanged if the ZED publisher honors the contract.
- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:123-181, 200-232, 1021-1106, 1340-1380, 1406-1472` — The entire publisher is sim/ROS-specific and must be replaced: rospy imports + recorder-module load pulling Gazebo topics (123-181; topics defined in save_data_img_depth_mask_pose.py:25-33), URDF FK pose interpolation + optical-frame offset + RobotMaskGenerator URDF mask render (710-783, 935-986; URDF_PATH at save_data_img_depth_mask_pose.py:39-42), Gazebo camera_info lazy-publish workaround + intrinsics fallback chain (374-509), depth republisher subprocess (650-700), /gazebo/pause_physics services (1215-1231).
    - CHANGE: New module needed: a ZED publisher (pyzed) reproducing exactly: (1) the SHM header/slot contract _HDR_FMT + _slot_layout (200-232, mirrored at live_shm_reader.py:57-106) — it is fully parametric on H/W so 1920x1200 or 960x600 16:10 needs no layout change; (2) the 'ready' JSON line with intrinsics from ZED left-rectified calibration (1368-1380); (3) the stdin/stdout command loop including capture_anchor (next-strictly-newer semantics, 1010-1019), start_recording/stop_recording with in-flight-write quiesce, num_recorded, build_init_pcd, shutdown, and ok:false for unknown ops (1406-1472 — unknown-op replies are what keep live_session's pause_gazebo calls non-fatal); (4) keyframe-dedup disk recording with atomic transforms.json swap writing fl_x/fl_y/cx/cy/w/h from intrinsics (1021-1106) — the _KeyframeFilter (309-347) and _write_frame_to_disk are already ROS-free and can be ported verbatim; (5) accept the reader's CLI args --live-root/--shm-name/--keyframe-translation-m/--keyframe-rotation-deg/--wipe-live-root (live_shm_reader.py:188-196). Scale: ~400-600 LOC reusing the ROS-free pieces of the existing 1478-LOC file. Pose = ZED VIO RIGHT_HANDED_Y_UP c2w written directly to the slot (no FK/slerp/optical-offset port); mask = all-255 array.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_session.py:206-207 (+ capture_only.py:112, live_ros_publisher.py:1084)` — Downstream code assumes clean depth (sim depth had no NaN/Inf). live_session._save_anchor_intrinsics_and_depth saves anchor.depth_m as RAW float32 TIFF for SAM3D — ZED NaN/Inf would propagate verbatim into SAM3D's metric pointmap. The uint16 disk writers do np.clip(d*1000,0,65535).astype(np.uint16): measured behavior NaN→0 (undefined-behavior cast, RuntimeWarning), +Inf→65535 i.e. a bogus valid-looking 65.535 m depth pixel. The TSDF paths gate at 0.05-3.0 m so they survive, but the naive fallback seed build_static_init_pointcloud only checks depth>0 (live_ros_publisher.py:1129,1149) and would back-project 65 m garbage.
    - CHANGE: Sanitize in the NEW ZED publisher before every SHM slot write: depth = np.nan_to_num(depth, nan=0.0, posinf=0.0, neginf=0.0) (0 = invalid is the existing convention everywhere downstream). With that single change, live_session, capture_only, fusion, and the ICP rewrite all work unchanged on ZED depth.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_session.py:95-150, 702-705, 944-946` — Gazebo physics pause/unpause: module globals + atexit unpause guard (95-150), pause_gazebo_physics(sub) before SAM3D (705), unpause in the finally block (946). With a ZED publisher these become dead code. They are already non-fatal — LiveShmSubscriber.pause_gazebo_physics catches everything and returns False (live_shm_reader.py:547-559), and the publisher command loop's unknown-op reply is ok:false — so the only effect is two misleading '[live] could not pause Gazebo' log lines per run, PROVIDED the new publisher answers unknown ops instead of staying silent (a silent publisher would block the reader for up to 600 s, live_shm_reader.py:446).
    - CHANGE: Either delete the pause/unpause call sites + atexit guard (~55 LOC) or gate them on a DGS_SIM=1 env var. Must NOT be removed from the reader contract until the ZED publisher's command loop is confirmed to reply to unknown ops.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/rewrite_transforms_with_icp.py:85, 97-101, 182` — ICP pose-refinement post-pass: the algorithm is input-agnostic and runs unchanged on a ZED dataset (reads intrinsics from transforms.json 55-59, defensively converts non-uint16 depth 138-140, mask>0 keep handles all-white masks 62-71/142-144, drives OnlineFusion 113-150). It remains valid — arguably MORE useful, since ZED VIO drift (cm-scale over a sweep) exceeds the URDF-FK 1-4 mm error it was built for, and the seed PLY still lives in the ICP-refined frame (Design Invariant #3). But the backup filename 'transforms_urdf_backup.json' (85) and pose_source tag 'icp_refined_from_urdf_v1' (182) are misnomers for VIO poses. Caveat: ICP correction is silently capped — coarse correspondence dist 0.05 m and fitness gate 0.30 (online_fusion.py:72-73, 289) mean any frame whose VIO pose is >~5 cm off keeps the raw pose; the printed drift stats (166-171) are the check.
    - CHANGE: Parameterize/rename the backup name and pose_source tag (e.g. transforms_raw_backup.json, 'icp_refined_from_zed_vio_v1'), ~5 LOC; eyeball the drift-stats output on the first real-camera dataset to confirm VIO error stays inside the 5 cm ICP basin.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:66 (+ rgbd_fusion_init.py:77)` — DEPTH_MIN_M/DEPTH_MAX_M = 0.05/3.0 truncation, tuned for the sim room: every real-world surface beyond 3 m is excluded from the TSDF seed, so the static splat has no Gaussians for far background and trains floaters against those pixels (compounded by the Gazebo-sky background color baked into the models). ZED min depth ~0.3 m makes the 0.05 floor moot.
    - CHANGE: Make DEPTH_MAX_M env-overridable (like DGS_TSDF_VOXEL_M at line 63) and raise it for real scenes, or accept seed-coverage loss for tabletop-only use. Mirror in rgbd_fusion_init.py DEPTH_TRUNC_M.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_session.py:477-489, 505-516, 524-526, 880` — VRAM orchestration is tuned around Gazebo holding ~2.6 GB: DGS_LIVE_DEFER_TSDF=1 default and the SAM3D-preload gating both cite Gazebo in their budget math; DGS_TSDF_VOXEL_M defaults to 0.003 for the deferred GPU TSDF (880) because of 1920x1200 hashmap OOM with sim co-resident. With no simulator, ~2.6 GB frees up.
    - CHANGE: No change needed to run; optionally re-measure and retune (concurrent TSDF may fit again, or 2 mm voxel at 1200p). Tuning only — defaults are conservative and safe on a real camera.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_session.py:1-23, 441-444, 571-572 (+ capture_only.py:1-5,168; bootstrap_live.sh:8)` — Cosmetic ROS/robot wording: docstrings say 'start ROS subscriber', logs say 'launching ROS publisher subprocess' and 'move the arm toward the object of interest' — for a handheld ZED the operator moves the camera, not an arm.
    - CHANGE: Reword prints/docstrings; zero functional impact.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/bootstrap_live.sh:27` — Default SAM3 prompt 'the can of coke on the table' is a Gazebo scene object (live_session.py:154 default 'oobject table' likewise).
    - CHANGE: Change the default prompt or rely on the operator always passing argv[2] (which bootstrap already supports).
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/capture_only.py:139-140` — --dynamic-fps default 30 matches the sim camera; ZED supports 60 fps at 960x600. Reader-side polling already handles any source rate (drops to the poll rate).
    - CHANGE: Optionally raise the default/document 60 fps for ZED; no correctness impact.

  _notes:_ Scale summary: 1 new module (ZED publisher, ~400-600 LOC, reusing the ROS-free slot-layout/_KeyframeFilter/_write_frame_to_disk pieces of live_ros_publisher.py) + ~15-25 LOC spawn-selection change in live_shm_reader.py (the one place that breaks the 'reader untouched' plan) + ~5 LOC NaN sanitize inside the new publisher + optional ~60 LOC of Gazebo-pause/wording cleanup in live_session.py. The six orchestration files need essentially no functional edits beyond that.

Out-of-area but confirmed by the CONTEXT and worth coordinating: (1) the Gazebo sky background (0.86,0.92,1.0) is baked into StaticGSModel/DynamicGSModel populate_modules — for real scenes this is a model-config change, not an orchestration change; it interacts with my DEPTH_MAX_M=3.0 finding (far background gets neither seed Gaussians nor a matching background color). (2) Design Invariant #2/#3 (camera-opt off because poses are ICP-refined to sub-mm) carries over to ZED only if the ICP rewrite (or in-stream ICP refinement) is actually applied — VIO drift is larger and time-correlated vs FK noise; if the per-frame VIO error ever exceeds the 0.05 m ICP correspondence ceiling (online_fusion.py:72) the refinement silently keeps the raw pose. Watch rewrite_transforms_with_icp's printed drift stats on the first real dataset, and reconsider camera-opt only if median drift stays high.

Design risks: ZED pose must correspond to the same sensor as intrinsics+depth (left rectified camera) — the SDK's positional tracking pose is configurable; getting this wrong reproduces exactly the misalignment class invariant #3 exists to fix. Timestamps: sim wrote ROS sim-time into the SHM stamp; nothing in these files consumes it except as opaque floats (capture_only adds its own wall stamp), so ZED ns-timestamps or wall time both work — just keep it monotonic since capture_anchor uses seq, not stamp. The publisher's intrinsics-fallback chain (cached transforms.json when camera_info times out) is a Gazebo-lazy-publish workaround and should NOT be ported — ZED calibration is always available from the SDK; porting it would risk silently running with a stale resolution after switching 1920x1200 <-> 960x600.

Untested assumption to verify on first hardware run: the ICP+TSDF GPU path at 1920x1200 with DGS_TSDF_VOXEL_M=0.003 was measured in-sim (live_session.py comments) but ZED depth noise (vs clean sim depth) may lower ICP fitness; the fitness gate (0.30) falls back to raw VIO poses, which is safe but means the seed inherits VIO drift.

  **Reusable (works as-is):**
  - capture_only.sh, bootstrap_live.sh, resume_live.sh contain no ROS at all — no roslaunch, no source /opt/ros, no topic waits, no pkill of ros nodes; they only pin the dynamic_gs conda env + CUDA toolchain and export DGS_LIVE_ROOT/DGS_SAM3_PROMPT/DGS_EAGER_ANYSPLAT. They run unchanged with a ZED source.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/capture_only.sh:32-48; /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/bootstrap_live.sh:36-74,88-146; /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/resume_live.sh:32-60)
  - The SHM contract is resolution-agnostic: header carries w/h/fx/fy/cx/cy and slot offsets, slot layout is computed from H/W, and the reader builds its numpy views entirely from the header — 1920x1200 or 960x600 (16:10, ~18.4 MB/slot x4) needs no layout change anywhere.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:204-232 (_slot_layout); /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:296-369 (intrinsics + slot views built from the ready message/header))
  - live_session's interactive flow is input-agnostic end-to-end: Enter-driven phase switching with headless EOF holdoff (_prompt_user), SAM3/FastSAM zero-mask retry loop, SamWorkerClient spawn + background segmenter/SAM3D preload, SAM3D thread, deferred-TSDF subprocess (already field-tested at 1920x1200 — the DGS_TSDF_VOXEL_M=0.003 default exists for that resolution), 3-tier seed fallback chain, dynamic-scene stub, warm-cache reuse, and timing ledger. Nothing reads robot state.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_session.py:231-257 (_prompt_user), 603-700 (retry loop), 501-569 (worker spawn/preload), 869-896 (deferred TSDF, 1200p-tuned voxel at 880), 897-932 (fallback chain), 948 (stub), 414-435 (warm cache), 57-92 (_has_complete_recording_cache checks files only))
  - The gripper blackout of the SAM3 anchor frame degenerates safely to a no-op with an all-white mask: rgb[~(mask>0)] = 0 touches zero pixels when mask is all-255.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_session.py:183-190 (keep = anchor.mask_keep > 0; rgb_rgb[~keep] = 0))
  - Every fusion consumer of the robot-exclusion mask no-ops correctly on all-white masks: depth[m==0]=0 zeroes nothing, and rgbd_fusion_init's inverted drop-mask returns all-False keep-everything.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/fusion_runner.py:88-91; /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:569-571; /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/rgbd_fusion_init.py:233-247)
  - transforms.json w/h/fl_x/fl_y/cx/cy are written from live intrinsics, never hardcoded — both the publisher's static-scene recorder and capture_only's dynamic-scene writer take them from the intrinsics object, so ZED calibration flows through automatically (no 800x800 assumption in any of the six files).  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:1026-1031; /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/capture_only.py:232-237)
  - The whole fusion stack is parametric on intrinsics passed at construction: ConcurrentFusionRunner forwards fx/fy/cx/cy/W/H into OnlineFusion; the deferred-TSDF subprocess and the ICP rewrite both re-read intrinsics from transforms.json. No camera model is baked in.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/fusion_runner.py:203-207; /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:478,556-559; /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/rewrite_transforms_with_icp.py:55-59,103)
  - The pose convention is already what ZED RIGHT_HANDED_Y_UP produces: OpenGL c2w in transforms.json/SHM, converted to OpenCV only internally at the fusion boundary via diag(1,-1,-1,1). A ZED VIO pose can be written into the pose slot verbatim; the unrecentered world frame is fine because the dataparser does no recentering/rescaling.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:495-498,523 (_cv_c2w applied internally); /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/rewrite_transforms_with_icp.py:20-29,73-78 (round-trip documented + implemented); online_fusion.py:30-33 (world frame unrecentered by design))
  - capture_only.py's two-phase recorder is publisher-agnostic: static phase only sends start/stop_recording over the control pipe and lets the publisher dedup; dynamic phase is pure reader-side SHM polling (peek_latest + seq dedup) at a wall-clock-paced loop, writing wall stamps itself — no dependence on /clock or sim RTF.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/capture_only.py:210-217 (static), 240-266 (dynamic poll loop), 122 ('stamp_wall': time.time()))
  - bootstrap stage 1.5 (rgbd_fusion_init) stays a cheap idempotent no-op on ZED runs: the deferred GPU TSDF touches the seed PLY mtime past transforms.json (and ConcurrentFusionRunner does the same), so _output_is_fresh skips the CPU rebuild.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_session.py:889-892 (seed_ply.touch()); /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/fusion_runner.py:320-326 (os.utime); /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/rgbd_fusion_init.py:254-274 (_output_is_fresh))
  - The SHM depth format already matches the ZED SDK natively: float32 metres in the slot (the publisher converted sim depth to float32 m precisely because that is the consumer-side contract), so a ZED publisher writes sl.MEASURE.DEPTH output straight in after NaN sanitization.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:126-141 (LiveFrame.depth_m float32 metres, documented), 361-363 (float32 slot view))
  - Keyframe dedup (2 cm OR 20°) is pure pose geometry with no robot/sim dependency — works identically for a handheld ZED sweep; thresholds are CLI-configurable from capture_only.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:309-347 (_KeyframeFilter, numpy only); /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/capture_only.py:141-144,169-174 (thresholds plumbed through))
  - resume_live.sh is fully input-agnostic: it only checks for the warm cache and execs ns-train dynamic-gs-live; the live pipeline's frame source is whatever publisher the reader spawns.  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts/resume_live.sh:20-30 (cache check), 53-60 (exec))

## AREA: reader/spawn (live_shm_reader.py)

- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:1084-1085, 1203` — The disk depth cast `np.clip(frame.depth_m * 1000.0, 0.0, 65535.0).astype(np.uint16)` assumes finite sim depth. ZED float32 depth carries NaN (occlusion), +Inf (too far), -Inf (too close): np.clip propagates NaN into an undefined uint16 cast, and +Inf clips to 65535 -> a fake 'valid' 65.535 m reading written to disk. Line 1203 writes the SAM3D anchor depth sidecar as raw float32 (`anchor.depth_m.astype(np.float32)`) with NO sanitization, so NaN/Inf reach sam3d.py directly in live mode. SHM frames are also written unsanitized (no nan_to_num/isfinite anywhere in the publisher or live_shm_reader, verified by grep).
    - CHANGE: In the new ZED publisher, sanitize once at the source before BOTH the SHM write and every disk/sidecar write: `depth = np.nan_to_num(depth_m, nan=0.0, posinf=0.0, neginf=0.0)` (0 = the pipeline-wide 'invalid' sentinel). This single change makes every consumer below safe by contract. ~3 LOC in the new publisher module.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/rgbd_decode.py:280, 84, 320, 348-401, 434` — decode_component_to_gaussians validity gate `valid_mask = mask_bool & (gt_depth_m > 0)` (line 280) excludes NaN (comparison is False) but PASSES +Inf. Inf then: (a) survives `_bilateral_smooth_depth` (line 84 `np.where(valid_np, depth_np, 0.0)` with valid=(>0)) and poisons cv2.bilateralFilter neighborhoods -> garbage normals; (b) the leak cut (line 320) only drops it where rendered depth is valid; (c) the cliff cut catches it only `if cliff_th > 0 and _HAS_SCIPY` (line 348) — otherwise line 401 passes the raw valid_mask and line 434 back-projects Inf depth into Inf-coordinate Gaussians inserted into the scene. Live SHM depth reaches this function unsanitized (dynamic_gs_model.py:1796-1811 _get_gt_depth does no filtering; pipeline_base.py:2158 passes it straight through).
    - CHANGE: Defense-in-depth even with publisher sanitization: change line 280 to `valid_mask = mask_bool & torch.isfinite(gt_depth_m) & (gt_depth_m > 0)` (1 LOC). Publisher sanitization alone is sufficient for correctness if the SHM contract is enforced, but this is the one consumer that inserts geometry permanently into the scene.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/sam3d.py:441-451` — _build_pytorch3d_pointmap: `valid = z > 0.0` (line 442) deliberately maps NaN/zero depth to NaN holes (line 450, SAM3D's _clip_pointmap handles them) — but +Inf passes the >0 test and produces +/-Inf xyz coordinates in the pointmap fed to SAM3D, breaking its pointmap normalization. Safe on the recorded path (depth comes from disk uint16 via phase0.py:561-562, cannot hold Inf) but the LIVE path feeds it the raw float32 anchor sidecar from live_ros_publisher.py:1203.
    - CHANGE: Change line 442 to `valid = np.isfinite(z) & (z > 0.0)` (1 LOC); pointmap[~valid] is already set NaN which is the convention SAM3D expects for holes.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/fusion_runner.py:97 (and online_fusion.py:289,300,422,433; stop_and_finalize 236-327)` — The ICP-refined pose returned by `fuser.add_frame(...)` is DISCARDED (line 97 ignores the return value) and stop_and_finalize never writes poses back to transforms.json. The seed PLY therefore lives in the ICP-refined frame while transforms.json keeps the RAW incoming poses. With sim FK this mismatch was sub-mm (median 0.96 mm, fixed by the optional rewrite_transforms_with_icp.py post-pass); with ZED VO drift it becomes cm-level — and per invariants #1/#2 (means LR=0, camera-opt off) static training has no mechanism to absorb a cm misalignment -> visibly smeared scene.
    - CHANGE: Collect `refined = self.fuser.add_frame(...)` per frame_idx in _FusionWorker (worker already has frame_idx), convert OpenCV->OpenGL (`refined @ diag(1,-1,-1,1)`), and rewrite each frame's transform_matrix in transforms.json inside stop_and_finalize (the file is already rewritten there for ply_file_path at lines 313-319). ~20 LOC in fusion_runner.py; supersedes the manual rewrite_transforms_with_icp.py post-pass which CLAUDE.md already lists as the intended future integration. Alternatively (minimal): make bootstrap run rewrite_transforms_with_icp.py mandatorily after capture.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:66, 242, 265, 366, 390-395` — DEPTH_MAX_M = 3.0 m hard cutoff (no env override, unlike TSDF_VOXEL_M) excludes everything beyond 3 m from BOTH the ICP source (CPU line 242, GPU line 366 depth_max) and TSDF integration (CPU depth_trunc line 265, GPU lines 390/395). In sim, beyond-3 m was sky (renders as the bg color anyway). In a real room, walls/furniture past 3 m get ZERO seed Gaussians — and with means LR=0 + densification OFF (NoRefineStrategy), that geometry can never be created during static training -> permanently missing background. No 20 m constant exists anywhere in these files; 3.0 m is the only far clip.
    - CHANGE: Add an env override mirroring the voxel knob: `DEPTH_MAX_M = float(os.environ.get("DGS_DEPTH_MAX_M", "3.0"))` (1 LOC) and raise it per real scene (ZED stereo depth degrades past ~5-8 m, so ~6 m is a sane real-world default). Note the interaction: a larger depth range grows the VoxelBlockGrid footprint, compounding the OOM item below.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:60-63` — TSDF_VOXEL_M defaults to 0.002 (2 mm; note CLAUDE.md's '1.5 mm' is stale — the code is 2 mm). The in-code comment already documents the exact failure for the ZED case: 'at 1920x1200/110deg the 2 mm VoxelBlockGrid hashmap OOMs a 16 GB GPU even with ~12 GB free'. ZED 2 is 110-degree diagonal FOV at 1920x1200 — precisely the cited configuration. The process aborts (Open3D CUDA OOM poisons the cache, per the __main__ note at lines 600-607).
    - CHANGE: No code change needed — set `DGS_TSDF_VOXEL_M=0.004` (or 0.005) in the ZED launch path for 1920x1200, or capture at 960x600. Additionally a tuning concern, not a code change: 2 mm voxel + 8 mm trunc (TSDF_TRUNC_M, line 64, CPU path only) were tuned for clean sim depth; ZED noise is several mm at 0.3-1.5 m, so a 3-5 mm voxel is likely the quality sweet spot anyway (noise > voxel makes finer grids pure overhead).
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_pipeline_base.py:209 (consumed at rgbd_decode.py:283-291)` — feedforward_rgbd_min_valid_fraction = 0.95: a FF component whose mask has <95% valid (nonzero) depth pixels is skipped ENTIRELY (rgbd_decode.py:283 returns skipped=True). Sim depth was 100% valid so this never fired; real ZED depth routinely has >5% holes (occlusion shadows, low-texture dropouts) in any region -> FF Mode B hole-fill would silently no-op on most components.
    - CHANGE: Lower the default (e.g. 0.6-0.7) for real-camera runs, or make it a per-backend default. Pure config/tuning, 1 LOC. The rest of the decode path already handles partial validity correctly: holes become cliff boundaries (rgbd_decode.py:357-360 treats invalid neighbours as cliffs) and sub-components <8 px are dropped (line 385).
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:72-73, 289, 422` — ICP fallback semantics tuned for trustworthy FK poses: when fitness < ICP_FITNESS_MIN=0.30 the frame is integrated at the RAW incoming pose ('trust the FK pose', line 73 comment). With sub-mm FK that is safe; with ZED VO a low-fitness frame means the drifted pose gets baked into the TSDF, smearing the model that all subsequent ICP registers against (compounding failure). Real depth noise also lowers fitness, so the fallback fires more often.
    - CHANGE: For ZED: on fitness < threshold, SKIP integration (drop the frame) instead of integrating at the raw pose, and log the fitness so drift-exceeds-basin events are visible. ~5 LOC in both add_frame implementations. Tuning companion: consider raising the coarse max-correspondence distance (ICP_STAGES[0], 0.05 m) if accumulated VO drift over a sweep approaches 5 cm — that is the hard capture basin (see reusable item on ICP drift absorption).
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/fusion/phase0.py:103, 114-121` — backproject_mask_to_world validity gate `mask_resized & (depth_np > 1e-4)` excludes NaN/zero but PASSES +Inf (and the 65.535 m fake-valid values the current publisher cast would produce from +Inf). The MAD scrub (lines 114-121) usually removes such outliers but is conditional (only when z.size>=10 and keep.sum()>=3) — not a guarantee. Currently safe because input depth comes from disk uint16 (phase0.py:375-377, 736-738), so this only bites if the disk contract is broken or 65535 mm values land on the object mask.
    - CHANGE: Defense-in-depth: add `& np.isfinite(depth_np)` at line 103 (1 LOC). Primary fix is the publisher sanitization (blocker item).
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:69-71, 253, 369, 430-431` — ICP_VOXEL_M=0.01 model voxel + NORMAL_RADIUS_M=0.03 normal estimation were tuned on clean sim depth. With several-mm ZED noise, point-to-plane normals on a 1 cm-voxel model get noisier, degrading per-frame ICP precision (the thing that must now absorb VO drift rather than sub-mm FK residual).
    - CHANGE: Tuning concern only (constants already exposed at module level). If real-capture ICP fitness is consistently low, try ICP_VOXEL_M=0.015-0.02 and a larger NORMAL_RADIUS_M before structural changes.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/online_fusion.py:82-83, 178-211` — adaptive_downsample constants NEAR_RADIUS_M=1.0 / FAR_VOXEL_M=0.01 were validated on a sim tabletop (validate_run_1). For a real room-scale scene the 1 m near-zone around the final viewpoint and 1 cm far voxel are tuning choices, not correctness issues. Also the docstring at line 197 claims 'default 5 mm' while the code is 0.01 (1 cm) — stale, as is CLAUDE.md's 5 mm claim.
    - CHANGE: Tuning pass on a real ZED capture (constants are already module-level knobs). Fix the stale '5 mm' docstring at line 197. No structural change.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/sam3d.py:28-29` — Hardcoded machine path `_CONDA_ROOT = Path("/home/mrc-cuhk/miniconda3")` for resolving the sam3 env python. Not sim-vs-real, but a portability hardcode discovered during the audit.
    - CHANGE: Derive from `CONDA_EXE`/`CONDA_PREFIX` env or keep as-is on this machine. Cleanup only.

  _notes:_ SHM-contract verdict for my area: the plan (keep the SHM contract + reader untouched, new ZED publisher producing byte-identical frames) is VIABLE for all five files — nothing here reads ROS, /clock, joint states, or sim time; everything consumes either SHM float32-m frames, disk uint16-mm/PNG files, or transforms.json. The one place 'byte-identical' is NOT sufficient is depth VALUES: the publisher must additionally guarantee finite depth (nan_to_num to 0) at the SHM boundary and before the uint16 disk cast, because three consumers (rgbd_decode.py:280, sam3d.py:442, phase0.py:103) gate on `> 0` which passes +Inf, and the existing clip-cast template (live_ros_publisher.py:1084) converts +Inf into a fake-valid 65535 mm disk value. Design risks / tuning concerns: (1) the ICP-refined-pose write-back (fusion_runner.py:97 discard) is the highest-leverage required change — under invariants #1/#2 (means LR=0, camera-opt off) there is no downstream mechanism to absorb cm-level VO-vs-seed misalignment; (2) the ICP drift-absorption ceiling is ~5 cm accumulated VO error relative to the model (ICP_STAGES coarse distance) with a silent raw-pose-integration fallback below fitness 0.30 — instrument fitness on first real captures; (3) VRAM: the code's own comment (online_fusion.py:60-63) flags 2 mm voxel OOM at exactly the ZED 1920x1200/110-deg configuration — set DGS_TSDF_VOXEL_M before first real run; real ZED noise (several mm at 0.3-1.5 m) makes 3-5 mm voxel the likely quality optimum anyway, so this is co-aligned, not a tradeoff. Out-of-my-area items surfaced while tracing (flagging for other agents, with evidence): live batch depth reaches change-mask building and _get_gt_depth with no isfinite anywhere (dynamic_gs_model.py:1796-1811; bilinear depth downscale at 1805-1810 would smear Inf if downscale>1); the Gazebo sky background color (model files) and the SAM3D anchor-sidecar write (live_ros_publisher.py:1203) are sim-coupled. Doc rot found: CLAUDE.md says TSDF voxel 1.5 mm and far-voxel 5 mm; code says 0.002 m (online_fusion.py:63) and 0.01 m (online_fusion.py:83, stale '5 mm' docstring at line 197). Open question: whether ZED VO should be lightly trusted at all vs. treating ICP as the primary pose source after frame 0 — current architecture uses the incoming pose only as ICP init + low-fitness fallback, which is the right shape, but the fallback polarity (integrate-at-raw-pose) should flip to skip-frame for VO input.

  **Reusable (works as-is):**
  - OnlineFusion's depth-validity gating is NaN-proof by construction on its input dtype: it consumes uint16 mm (cannot encode NaN/Inf), and both ICP source and TSDF integration apply a min/max range gate that excludes 0 (invalid sentinel) — CPU: `(depth_u16 > 50mm) & (depth_u16 < 3000mm)`; GPU: depth_max=3.0 in create_from_depth_image and voxel_grid.integrate (Open3D treats <=0 as invalid). Works unchanged if the ZED publisher writes invalid->0 uint16 mm.  (online_fusion.py:242 (CPU gate), 261-268 (depth_trunc), 360-367 (GPU depth_max + stride), 388-396 (GPU integrate))
  - Units are consistent end-to-end: uint16 mm on disk / float32 m in memory. DEPTH_SCALE=1000 converts disk->m inside Open3D; fusion_runner reads disk uint16 verbatim; phase0 converts via the dataparser's depth_unit_scale_factor; the SAM3D sidecar is written float32 m and read float32 m; rgbd_decode consumes float32 m from SHM. No double-scaling anywhere.  (online_fusion.py:65 (DEPTH_SCALE), fusion_runner.py:84-87 (uint16 read), phase0.py:370+376 and 732+737 (depth_unit_scale_factor), phase0.py:561-562 (float32-m sidecar write) matching sam3d.py:713 (float32 read), live_shm_reader.py:129-138 (SHM float32 m contract))
  - No hardcoded intrinsics anywhere in the five files — everything flows from the intrinsics object (camera_info today, ZED calibration tomorrow) or transforms.json/Cameras, with correct per-axis rescaling when render resolution differs from source resolution (handles non-square 16:10 ZED frames).  (fusion_runner.py:200-207 (intrinsics object), online_fusion.py:557-559 (transforms.json fl_x/fl_y/cx/cy/w/h), phase0.py:123-137 and 173-184 (camera intrinsics + sx/sy rescale), rgbd_decode.py:41-55 (_camera_intrinsics), sam3d.py:432-435 (intrinsics JSON sidecar))
  - The per-frame ICP exists to absorb exactly the incoming-pose error class ZED VO produces, and does so within a ~5 cm basin: the ICP source is back-projected into world at the RAW incoming pose, registered point-to-plane against the global model (anchored at frame 0, refreshed every 5 frames), and the correction is composed as `T @ c2w_cv`. Coarse stage max-correspondence distance 0.05 m / 6 iters, fine 0.02 m / 12 iters. Accumulated VO drift up to a few cm relative to the refined model is absorbed; beyond ~5 cm correspondences fail and fitness drops below 0.30 (then see the fallback caveat in sim_specific).  (online_fusion.py:72 (ICP_STAGES=((0.05,6),(0.02,12))), 282-289 (CPU registration + T@c2w composition), 341-346 + 411-422 (GPU multi_scale_icp with same distances), 363 (source built at raw pose), 294-299 + 427-432 (model refresh every MODEL_REFRESH_EVERY=5))
  - All-white robot mask (no arm in view) is a clean no-op in every consumer: fusion zeroes depth only where mask==0 (zeroes nothing), phase0a's gripper blackout keeps every pixel, and the frame watcher tolerates a missing mask_path entirely.  (fusion_runner.py:88-91 (depth[m==0]=0), online_fusion.py:569-571 (same in fuse_recorded_dataset), phase0.py:394-407 (keep-all blackout no-op), fusion_runner.py:133-136 (mask_path optional))
  - The single OpenGL->OpenCV conversion site for fusion, `c2w @ diag(1,-1,-1,1)`, is exactly correct for ZED positional tracking under COORDINATE_SYSTEM.RIGHT_HANDED_Y_UP (camera frame X-right/Y-up/Z-back = OpenGL c2w). All other geometry code (phase0 back-projection, cull_points_in_front, rgbd_decode) works directly in the OpenGL/Nerfstudio convention against transforms.json/Cameras poses and is world-frame-agnostic — the pipeline never assumes a gravity-aligned or robot-base world frame (dataparser is orientation/center 'none').  (online_fusion.py:495-498 (_cv_c2w), phase0.py:139-148 (OpenGL back-projection), phase0.py:185-211 (projection inverse with isfinite guards at 198/206), rgbd_decode.py:7-16 + 104-116 + 417-421 (validated OpenGL convention))
  - cull_points_in_front is fully robust to NaN/Inf inputs — both the target depth buffer build and the query points are gated on np.isfinite(z) & (z > 0).  (phase0.py:198 (tvalid gate), phase0.py:206 (in_img gate))
  - Nothing in the five files assumes a square or 800x800 frame: H/W come from the intrinsics object or the images themselves, masks and depth are resized when shapes mismatch, and the SAM3D path resizes to max_side=518 regardless of aspect. 1920x1200 / 960x600 flow through unchanged (modulo the VRAM tuning items).  (online_fusion.py:223-226 + 320-326 (W/H from ctor), phase0.py:76-101 (mask/RGB resize to depth shape), rgbd_decode.py:255-274 (shape-driven), sam3d.py:399-414 (_resize_image_and_mask aspect-preserving))
  - The MAD-based depth-outlier scrub in backproject_mask_to_world (silhouette-edge depth bleed removal) was built for exactly the artifact real stereo depth produces at mask boundaries — it becomes MORE useful with ZED, not less. Same for rgbd_decode's leak cut, whose own comment cites real-sensor (RealSense/TOF) 1-5 cm table-edge bleed as its design case.  (phase0.py:109-121 (MAD scrub + rationale comment), rgbd_decode.py:296-303 (leak-cut comment citing real sensors))
  - rgbd_decode's cliff cut already treats invalid (depth<=0) neighbours as depth cliffs and re-segments into per-surface sub-components, so properly-sanitized ZED holes (->0) degrade gracefully into smaller decoded patches rather than corrupting normals.  (rgbd_decode.py:352-388 (invalid_left/invalid_up cliff marking at 357-360, relabel + <8px drop at 381-385))
  - The SAM3D generation path is input-source-agnostic: it consumes an RGB PNG, binary mask PNGs, an optional float32-m depth TIFF and an intrinsics JSON — all file-based contracts with no sim/ROS coupling. NaN depth pixels are deliberately propagated as pointmap holes for SAM3D's _clip_pointmap. (Only the +Inf gate needs the 1-LOC fix listed in sim_specific.)  (sam3d.py:702-719 (file-based inputs), sam3d.py:449-451 (NaN-hole convention), sam3d.py:918-992 (subprocess contract, paths only))
  - ConcurrentFusionRunner's watcher/worker architecture is fully decoupled from the frame source: it watches transforms.json growth and reads rgb/depth/mask files — a ZED publisher that writes the same on-disk keyframe format (uint16-mm depth TIFF, BGR PNG, atomic transforms.json swap, numeric filename suffixes for ordering) drives it byte-identically with zero changes.  (fusion_runner.py:106-164 (watcher reads only transforms.json + file paths), fusion_runner.py:75-103 (worker reads files, BGR->RGB at 94-96), online_fusion.py:561 (numeric-suffix frame ordering in the recorded post-pass))

## AREA: orchestration (live_session/scripts)

- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/static_gs_model.py:64-65, 165-172, 204-207` — Gazebo sky background baked in: `use_simulator_background: bool = True` + `simulator_background_rgb = (0.86, 0.92, 1.0)`. `populate_modules` calls `set_background(sky)` and `_get_background_color` returns it unconditionally, overriding Splatfacto's `background_color="random"` path. During static training the render is composited `rgb = render + (1-alpha)*sky` (splatfacto.py:582-584) while 3-channel GT passes through `composite_with_background` unchanged (splatfacto.py:618-630, only composites 4-channel). In sim this is a free lunch (GT sky pixels ARE that color); with a real camera every GT pixel is real scenery, so any pixel the TSDF seed doesn't cover (ZED depth holes: NaN/Inf/0 regions produce no seed points) gets a constant light-blue bias the model can only fight via opacity/scale smearing (means frozen, densify off via NoRefineStrategy).
    - CHANGE: Flip `use_simulator_background` default to False (or set it in the method configs) so `_get_background_color` falls through to Splatfacto's. Then either keep `background_color="random"` for static training (random bg per step drives opacity up to cover all real GT — standard Splatfacto treatment for real scenes) or set `background_color="black"` for a deterministic bg. One-line config default change per model; no code change.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_model.py:60-61, 549-552, 677-680, 2205-2206, 2210, 2232, 2239` — Same sky background in DynamicGSModel — and here it also affects dynamic-phase rendering: `get_outputs` composites main rgb (2205-2206) AND the three subset renders `flagged_rgb`/`non_flagged_rgb`/`non_inserted_rgb` (2208-2239) against the sky color. The CDN 'rgb' mode compares rendered-vs-live: uncovered/partially-covered pixels render sky-tinted while live shows real background → false-positive change regions. Fully-empty pixels are saved by `change_mask_coverage_threshold=0.5` (config lines 133-139, gated in change_detection/change_mask.py:155-162), but partial-alpha pixels (0.5<alpha<1) still carry `(1-alpha)*sky` tint that the coverage gate does NOT exclude.
    - CHANGE: Same flag flip as StaticGSModelConfig (the two configs are documented as kept-in-sync). For the dynamic phase prefer a DETERMINISTIC bg (`background_color="black"` or keep a fixed neutral via set_background) — `"random"` returns a new random color every training-mode render (splatfacto.py:455-457) and the dynamic tick renders in training mode, which would inject per-tick random tint into the CDN comparison. Keep the coverage gate as-is.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/static_gs_model.py:81 (and dynamic_gs_model.py:457)` — `sam3_prompt_text: str = "the can of coke on the table"` — a Gazebo-scene object as the default text prompt for Phase 0a segmentation (FastSAM+CLIP scores ALL mask candidates against this text; SAM3 grounds on it). On a real scene without a coke can, segmentation targets the wrong object or returns junk → Phase 0b fuses the wrong thing or nothing.
    - CHANGE: Always pass the real prompt (`--pipeline.model.sam3_prompt_text` / bootstrap_live.sh's sam3_prompt arg already supports this). Optionally change the default to something neutral/empty + a loud warning when unset. Zero-LOC if treated as operator discipline; 1-line default change otherwise.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_config.py:47-53 (also 105 for preseg)` — `camera_optimizer=CameraOptimizerConfig(mode="off")` for static training is justified by invariant #3: transforms.json contains ICP-refined poses with sub-mm residual (measured 0.96 mm median on URDF FK). ZED VO is good but drifts (mm-to-cm over a sweep); raw VO poses written to transforms.json would NOT be sub-mm-consistent with the TSDF seed, and with camera-opt off there is nothing to absorb the residual → smearing. The OnlineFusion per-frame ICP itself is input-agnostic (it registers each frame against the TSDF model regardless of where the init pose came from), so the seed lives in the ICP-refined frame either way — what matters is the write-back.
    - CHANGE: Keep mode="off" (do NOT silently violate invariant #2), but make the ICP write-back (`scripts/rewrite_transforms_with_icp.py`, or its planned integration into ConcurrentFusionRunner.stop_and_finalize) MANDATORY in the ZED capture flow, since VO init error is strictly larger than URDF FK error. Measure residual drift on the first real capture; only if post-write-back median residual is well above the ~1-4 mm URDF baseline reconsider camera-opt (and flag it as an invariant change). No change in these files.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_model.py:1796-1811, 2285-2286, 1862-1863` — Model depth consumers assume CLEAN, finite depth (sim depth was clean). `_get_gt_depth` bilinearly interpolates `batch["depth_image"]` when downscaled — bilinear smears NaN over neighborhoods; `masked_l1_depth_loss` (2285-2286) produces NaN loss if any masked pixel is NaN (harmless to params at LR=0 but poisons logged metrics); depth-mode CDN (`change_mask_mode='depth'/'depth_outlier'`) treats gt=0 as a huge |pred-gt| error → false-positive change. ZED depth has NaN/Inf/0 invalid pixels.
    - CHANGE: Sanitize at the new ZED publisher (the right place under the keep-SHM-contract plan): map NaN/Inf to 0.0 in the float32-metres SHM frame and to 0 in the uint16-mm on-disk TIFF, so downstream sees the same 'invalid=0' convention sim depth used. If depth-mode CDN is ever enabled on real data, additionally gate gt>0 & isfinite inside the comparison (small patch in change_detection). Default CDN mode is 'rgb' (dynamic_gs_model.py:77, 95-97) so out-of-box runs are unaffected.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_model.py:247-254` — `xfeat_ransac_inlier_threshold: float = 0.025` — docstring explicitly sizes 25 mm for 'Gazebo openni_kinect depth' modeled noise + bilinear depth interpolation. ZED neural/ultra depth noise at 0.5-3 m is in a similar regime but not identical (grows with distance squared, plus invalid-pixel borders).
    - CHANGE: Re-verify on a real capture via the existing median-residual diagnostic log before changing; adjust the default only if measured residuals are consistently outside 10-20 mm. 1-line tune at most.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_model.py:211-222, 396-408, 409-419` — Pixel-unit tracker knobs tuned at 800x800 sim resolution: `xfeat_top_k=3000` (keypoint budget sized for 800x800 frames), `xfeat_crop_padding_px=150` (sized vs '~30 px per-tick motion' and XFeat's ~64 px receptive field at 800x800), `xfeat_object_search_radius_px=80`. At 1920x1200 angular pixel density is ~1.5-2.4x higher: per-tick motion in px scales up, the crop covers proportionally less context, and extract cost grows with image area (DN.3c_xfeat_extract must stay under the ~100 ms budget per the 211-222 docstring).
    - CHANGE: If running ZED at 960x600 the values are near sim scale — likely fine as-is. At 1920x1200, scale the px knobs ~1.5-2.4x and re-check DN.3c_xfeat_extract in timing_report.txt (or downscale frames before the tracker). Config-only retune, no code.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/static_gs_pipeline.py:360-363, 403-426` — Early-stop thresholds tuned on sim renders: `STATIC_EARLY_STOP_LOSS=0.09` chosen because 'the full-res main_loss EMA plateaus at ~0.08' on sim imagery. Real-camera frames (sensor noise, exposure variation, motion blur, rolling shutter) plateau at a higher photometric loss, so the early stop may never fire — benign (runs the full 500-step budget, <20 s) but the 2.6 s fast path is lost; conversely if the real plateau happens to dip under 0.09 transiently the EMA+patience still protects.
    - CHANGE: Measure the EMA plateau on the first real capture and adjust via the existing `DGS_STATIC_EARLY_STOP_LOSS` env override; bake a new default only after measurement (per the no-guessing rule). 0-LOC (env var) or 1-line.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/anysplat_decode.py:626, 657-661` — (Adjacent to my area, found while tracing the sky constant.) `reproject_anysplat_to_scene(background_rgb=(0.86,0.92,1.0), background_tol=0.08)` drops FF-decoded Gaussians whose DC color is within tol of the Gazebo sky. On real input the filter mostly goes inert (no constant bg) — BUT any real surface in that pastel-blue band (each channel within 0.08) is silently stripped from FF inserts; call sites don't override the default.
    - CHANGE: Gate the bg-color drop behind the model's `use_simulator_background` (or pass background_rgb=None to disable when sim bg is off). ~5 LOC.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_model.py:74-75, 100-110` — Depth-mode CDN constants carry sim/ICP calibration: `change_mask_depth_threshold=0.02` (2 cm fixed, 'NOT robust to bulk pose drift') and `change_mask_outlier_median_multiplier=15.0` ('calibrated for ICP-refined poses with drift below ~5 mm'). ZED depth noise at 2-3 m can exceed 2 cm and VO drift changes the outlier baseline. Inert at defaults (mode='rgb', line 77/95-97).
    - CHANGE: Only if depth CDN modes are enabled on real data: recalibrate the multiplier/threshold against measured ZED depth-error statistics. No change for default config.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_config.py:177, 219, 192-200, 226-229` — Dynamic methods set `CameraOptimizerConfig(mode="SO3xR3")` — inert in practice (camera_opt LR=0 via _ZERO_LR_OPTIMIZERS line 145, and `_should_apply_camera_optimizer` requires phase=="static", dynamic_gs_model.py:1187-1201) but misleading. Also the dynamic-gs-live comments/description hardcode 'ROS publisher subprocess (live_ros_publisher.py in the dynamic_gs_ros conda env)' — doc rot once the ZED publisher exists.
    - CHANGE: Cleanup: change mode to "off" for the two dynamic methods (behavior-identical) and update the live-method comment/description to name the frame-source abstraction instead of ROS. ~6 lines of config/docs.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/scripts (diag/utility scripts):regenerate_static_init_ply.py:47-48, static_render_psnr.py:15, diag_tracker_input.py:53, merge_anysplat_with_scene_ply.py:89` — Four ad-hoc/diagnostic scripts hardcode the Gazebo sky color (PSNR background, sky-pixel filtering, tracker-input diagnostics, PLY merge drop-color). Not on the runtime path.
    - CHANGE: Parameterize or ignore until used on real data.

  _notes:_ Scope: the four assigned files plus directly-traced helpers (splatfacto.py background/composite, dynamic_gs_datamanager.py dataparser block, change_detection/change_mask.py, utils/active_mask.py, utils/anysplat_decode.py). Verdict on the working plan: nothing in models/configs/static-pipeline makes the byte-identical-SHM plan impossible — no blockers found in this area; the model layer is camera-source-blind. The required items are (a) the sky-background default, (b) the coke-can prompt default, (c) the ICP-write-back-to-transforms.json process requirement that keeps camera_optimizer=\"off\" valid, and (d) depth sanitation at the new publisher (NaN/Inf→0) since every depth consumer here assumes finite values. Design risks / open questions: (1) Background choice interacts with the frozen-geometry invariants — with means LR=0 and densification OFF, static training CANNOT create geometry to cover seed holes, so whatever bg policy is chosen, holes stay holes; \"random\" bg pushes existing-Gaussian opacities up (mild smear risk at hole borders), \"black\"/fixed gives deterministic renders for the CDN; I lean black/fixed-neutral for CDN stability but this should be A/B'd on the first real capture (per the validate-by-A/B memory rule). (2) Invariant #6 (sky bg) becomes obsolete for real input — CLAUDE.md should be updated rather than silently contradicted, and note the same constant also lives outside my area in viser-direct defaults and nerfstudio/viewer/control_panel.py per the invariant text (my repo grep of control_panel.py found no literal hit, so verify which viewer default actually carries it). (3) The camera-opt question is a measurement task, not a code task: ZED VO + OnlineFusion ICP should keep poses self-consistent with the seed exactly as URDF+ICP did, but the 0.96 mm median drift number was measured with URDF FK initialization — re-measure with VO init before trusting sub-mm. (4) Tuning concerns are concentrated in pixel-unit tracker knobs (top_k / crop padding / search radius) and the early-stop loss threshold — all config-level, all should be re-measured (timing_report.txt, median-residual log) rather than guessed. (5) The `sam3_reuse_cached` / `reuse_sam3d_generated_ply` caches are dataset-dir-scoped, so fresh ZED datasets are safe; only reusing an old sim dataset dir would serve stale sim artifacts.

  **Reusable (works as-is):**
  - The four files contain zero ROS / SHM / Gazebo runtime coupling — models consume only nerfstudio `Cameras` + batch dicts (`image`, `depth_image`, `mask`), so a byte-identical SHM publisher swap never touches them.  (dynamic_gs_model.py:1843-1863 (prepare_dynamic_update consumes camera+batch only), 782-791 (get_live_rgb takes batch), static_gs_pipeline.py:55-59 (imports: datamanager/trainer/fusion/persistence only — no ROS))
  - Dataparser settings remain correct for ZED: depth_unit_scale_factor=1e-3 matches the uint16-mm on-disk format (publisher converts to float32 m only at the SHM boundary), and orientation_method="none" / center_method="none" / auto_scale_poses=False keep poses metric and unrecentered — exactly right for ZED RIGHT_HANDED_Y_UP OpenGL c2w written straight into transforms.json.  (dynamic_gs_datamanager.py:78-84 (NerfstudioDataParserConfig block), :71-73 (keyframe thresholds documented as metric because auto_scale_poses=False))
  - All-white robot-exclusion masks (no arm in view) are a clean no-op everywhere these files touch masks: static loss multiplies GT and pred by the mask (all-ones = identity); `_get_batch_mask` just resizes; the CDN gripper-keep erosion preserves an all-white mask (erode is implemented as inverted max_pool2d with zero padding, so borders stay 'keep').  (dynamic_gs_model.py:2273-2275 (static loss delegates to splatfacto, which applies batch mask at splatfacto.py:665-671), dynamic_gs_model.py:1813-1828 (_get_batch_mask), utils/active_mask.py:43-52 (erode_binary_mask padding semantics), change_detection/change_mask.py:135-149 (gripper keep path))
  - Nothing in the four files hardcodes 800x800 or assumes a square aspect: width and height are read and handled independently in every render path, the CDN auto-downsample sizes itself from the actual resolution, the static config trains full-res from step 0, and the dynamic pipelines force schedules to full-res via _step_offset — so 1920x1200 / 960x600 16:10 frames flow through unchanged.  (dynamic_gs_model.py:2164-2168 (separate width/height), :113-127 (change_mask_downsample_factor=0 auto, target_side scaling), static_gs_model.py:71-77 (num_downscales=0), dynamic_gs_pipeline_base.py:516-522 (_step_offset=10_000 forces full-res schedules))
  - Camera-optimizer is provably inert in the dynamic phase regardless of input source: `_should_apply_camera_optimizer` requires training AND phase=="static" AND mode!="off", and the dynamic methods zero camera_opt LR — so ZED VO c2w poses are used raw per tick, exactly as sim poses were.  (dynamic_gs_model.py:1187-1206 (_should_apply_camera_optimizer / _get_optimized_camera_to_world), dynamic_gs_config.py:138-146 (_ZERO_LR_OPTIMIZERS camera_opt lr=0.0))
  - The persistent identity-buffer machinery (object_flags / object_instance_ids / sam3d_init_target_flags / inserted_flags), insert/delete + optimizer-refresh, state-dict resize-on-load, and the Phase 0b subset queries are purely geometric/index-based and carry no sim assumption.  (static_gs_model.py:177-196 (buffer registration), :223-265 (load_state_dict resize), :271-426 (resize/insert/delete), :449-604 (mask-slab + object-subset queries); dynamic_gs_model.py:553-592, 958-1185 (same machinery + FF insert))
  - The static pipeline orchestration (Phase 0a at construction, AFTER_TRAIN Phase 0b + static_state.pt save, timing ledger/report, eager-AnySplat spawn) is entirely dataset-dir-driven and input-agnostic.  (static_gs_pipeline.py:129-161 (Phase 0a + eager AnySplat keyed off datamanager.data), :194-251 (_finalize_static_training → run_phase0b_fusion → save_post_fusion_state at <data>/static_scene/static_state.pt))
  - Segmentation backend default `fastsam` (FastSAM-x + CLIP) and the SAM3 fallback are real-photo-trained models invoked on whatever RGB the dataset provides — they work unchanged (arguably better) on real camera frames; only the text prompt is scene-specific.  (static_gs_model.py:92-103 (segmentation_backend + fastsam knobs), dynamic_gs_model.py:467-474 (kept-in-sync copy))
  - Rasterization clip planes are generic, not sim-tuned: near_plane=0.01 / far_plane=1e10 in every gsplat call comfortably bracket ZED's ~0.2-0.3 m minimum depth and any indoor range.  (dynamic_gs_model.py:2101-2102 (render_object_mask), 2194-2195 (main render), 2223-2224 (subset renders))
  - Live RGB ingestion is format-agnostic: `get_live_rgb` normalizes uint8→float and passes 3-channel images through `composite_with_background` untouched (the bg only ever composites 4-channel images), so real ZED RGB needs no model-side change.  (dynamic_gs_model.py:782-791 (get_live_rgb), nerfstudio/models/splatfacto.py:618-630 (composite_with_background shape[2]==4 gate))
  - The CDN scene-coverage gate already defends against renders of regions the warm-cache scene doesn't cover (alpha < 0.5 pixels excluded from change detection) — a real-camera robustness feature that works as-is and reduces (but does not eliminate) the impact of the background-color issue.  (dynamic_gs_model.py:133-139 (change_mask_coverage_threshold=0.5 config + threading note), change_detection/change_mask.py:151-162 (gate implementation))

## AREA: publisher (live_ros_publisher.py)

- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:123-127, 430-431, 606-640, 810-857` — Frame ingest is rospy: init_node, message_filters Subscribers on /dynaarm_arm/dynaarm_arm/camera1/image_raw/compressed (JPEG) + raw depth topic, ApproximateTimeSynchronizer (slop 0.02 s) feeding a drop-oldest queue + worker thread.
    - CHANGE: New module (zed_live_publisher.py): a grab() loop on sl.Camera — grab() returns hardware-synced RGB+depth so the synchronizer disappears entirely; the grab thread becomes the producer for the existing worker/queue (or writes SHM directly). All SHM/recording/command-loop code ports verbatim.
- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:373-426, 446-509` — Intrinsics come from /camera_info with a Gazebo lazy-publish priming subscriber and a 4-level fallback chain (transforms.json, ~/.cache/dgs_camera_intrinsics.json, datasets glob).
    - CHANGE: Read fx/fy/cx/cy/width/height from ZED calibration (camera_configuration.calibration_parameters.left_cam of the RECTIFIED left camera, at the configured resolution). Drop the priming + fallback chain. These values feed the header pack, the ready JSON, slot sizing, the PCD back-projection, and recorded transforms.json — single source, set once.
- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:607-612, 702-744, 746-783 (helpers: save_data_img_depth_mask_pose.py:84,140-161,473-483)` — Pose chain is sim-only: Gazebo pose-plugin topic + joint-states topic, 50 Hz throttle (_POSE_JOINT_MIN_DT_SEC=0.02), bisect+slerp interpolation to the image stamp, URDF zero-config static offset camera_pose_link->camera_link_optical, then rotate_camera_frame_only (R @ diag(1,-1,-1)) to get an OpenGL c2w.
    - CHANGE: Enable ZED positional tracking with COORDINATE_SYSTEM.RIGHT_HANDED_Y_UP; per grab, zed.get_position(REFERENCE_FRAME.WORLD) is already the OpenGL c2w of the camera in a metric world — write its 4x4 (float64) straight into the slot. No interpolation (pose is sampled at the grabbed frame), no URDF, no optical flip. Pose convention MUST match or every downstream consumer (TSDF fusion, splat training, XFeat back-projection) breaks.
- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:154-159, 177-226` — The reader's spawn wrapper hardcodes _PUBLISHER_SCRIPT=live_ros_publisher.py, the dynamic_gs_ros env python, and wraps the command in `source /opt/ros/noetic/setup.bash` + PYTHONNOUSERSITE=1 + LD_LIBRARY_PATH stripping.
    - CHANGE: Point _PUBLISHER_SCRIPT/env at the new ZED publisher (pyzed env, no ROS sourcing). This is the only reader edit required — the SHM attach/peek/control-pipe code (reader 257-580) is publisher-agnostic. The 'reader untouched' plan holds for everything except these spawn constants.
- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:867-870 (double-spawn documented), 982 (absolute world pose written per frame)` — The bootstrap flow spawns TWO publishers (capture, then teleop). With Gazebo, pose is absolute sim-world FK so a respawn yields the same frame. ZED VIO zeroes its world frame at each tracking start, so the second publisher's poses would be in a different frame than the static map built by the first.
    - CHANGE: Pick one: (a) single resident ZED publisher across both phases (preferred — no SDK feature dependency), (b) ZED area-memory: save area file on capture shutdown, load + wait for relocalization in the teleop spawn, or (c) post-hoc rigid alignment of the new VIO frame onto the TSDF seed via ICP before going live.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:774-781, 953-965 (generator: save_data_img_depth_mask_pose.py:279-598)` — Robot-exclusion mask is rendered per frame via URDF FK + pyrender from interpolated joint states (also lazily constructs RobotMaskGenerator inside _interpolate_c2w just to get the static link offset).
    - CHANGE: With no arm in view, write mask = np.full((H, W), 255, np.uint8) once and reuse (255 = keep is the established convention, live_ros_publisher.py:364-367). All consumers degrade gracefully: save_anchor_for_sam3's blackout becomes identity (1185-1193), gripper_keep filtering keeps everything. This also deletes the urdfpy/pyrender dependency and the ~17 ms/frame render cost.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:785-808, 945-946, 985` — Decoders assume clean sim data: cv2.imdecode of JPEG CompressedImage for RGB; raw 32FC1 reinterpret for depth with NO invalid-pixel handling (Gazebo depth was clean, ZED depth has NaN/Inf/0).
    - CHANGE: ZED: retrieve_image(VIEW.LEFT) is BGRA -> slice to BGR uint8 (SHM contract is 3-channel BGR); retrieve_measure(MEASURE.DEPTH) float32 metres, then SANITIZE: d[~np.isfinite(d) | (d <= 0)] = 0.0 BEFORE the SHM write. 0.0 is the established invalid marker (validity tests are depth>0 at 1129/1149; the recorder's own sanitizer maps ~isfinite|<=0 to 0 at save_data_img_depth_mask_pose.py:127-129). Unsanitized NaNs poison TSDF integrate, ICP, XFeat back-projection and RANSAC.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:1084` — Disk depth write does np.clip(frame.depth_m * 1000.0, 0, 65535).astype(np.uint16): np.clip propagates NaN and float->uint16 cast of NaN is undefined (platform garbage). Never mattered on sim depth; guaranteed to fire on ZED depth.
    - CHANGE: Either rely on source sanitization (previous item) or harden here: zero non-finite values before the clip (mirror save_data_img_depth_mask_pose.py:127-129). +Inf is incidentally safe (clips to 65535) but should also become 0 (=invalid), not 65.5 m.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:938, 970, 1039` — Frame stamp is ROS sim time (image_msg.header.stamp.to_sec()); rospy.Time.from_sec wraps it for the (unused) stamp arg of _write_frame_to_disk. Sim-time advances at RTF rate and starts near 0.
    - CHANGE: Use the ZED image timestamp as float seconds (zed.get_timestamp(TIME_REFERENCE.IMAGE).get_nanoseconds() * 1e-9). Monotonic seconds is all the contract needs (slot stamp f64, anchor identity, replay ordering). Drop rospy.Time entirely — _write_frame_to_disk never reads its stamp parameter.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:1215-1231, 1462-1465` — pause_gazebo/unpause_gazebo ops call /gazebo/pause_physics services. The reader DOES send these (live_shm_reader.py:549,556; live_session.py:705,946 pauses physics around SAM3D + PCD build to free CPU/GPU).
    - CHANGE: Keep both ops in the command loop, reply {"ok": false} (or true) without doing anything — live_session already tolerates failure ('could not pause Gazebo', live_session.py:115-122, continues). Do NOT remove the ops: an unknown_op error reply would also be tolerated but the no-op stub is the faithful contract. Note the lost benefit: on sim, pause freed compute during SAM3D; on real hardware there is nothing to pause.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:136-181` — Imports the recorder module (save_data_img_depth_mask_pose.py) for helpers AND for sim constants/classes; that module imports rospy, trimesh, pyrender, urdfpy at top level, so the ZED publisher cannot import it in a ROS-free env.
    - CHANGE: Extract or inline the 5 ROS-free helpers the new publisher still needs: write_ascii_ply, distribute_point_budget_evenly, load_saved_depth_mm/load_saved_mask/load_saved_rgb, CameraIntrinsics, plus the constants INIT_CLOUD_NAME, MAX_INIT_CLOUD_POINTS, IMAGE_NAME_PREFIX. Everything else imported at 151-181 (topics, RobotMaskGenerator, ros_image_to_bgr, ros_depth_to_uint16_mm, pose_msg_to_matrix, rotate_camera_frame_only, sync constants) is sim-only and disappears.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:26-47, 88-121` — Python 3.8-only syntax constraint, dynamic_gs_ros env sys.path forcing, numpy deprecated-alias shims for urdfpy, and pyparsing import debugging — all artifacts of the ROS Noetic env.
    - CHANGE: Drop all of it; the ZED publisher runs in a modern env (any python with pyzed + numpy + opencv). KEEP the fd-1 dup / dup2-stderr IPC protection at lines 63-67 — the ZED SDK can print to stdout (sdk_verbose, driver warnings) and would corrupt the JSON pipe exactly like rospy did.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:749-750, 942-943, 1014` — Frames are silently dropped until the first Gazebo pose sample exists (_interpolate_c2w returns None); capture_anchor's wait loop also conditions on rospy.is_shutdown().
    - CHANGE: ZED equivalent: gate SHM publishing on POSITIONAL_TRACKING_STATE == OK (publish nothing while SEARCHING/OFF, exactly mirroring the None-gate so the reader's wait_for_first_frame semantics hold); replace rospy.is_shutdown() with the publisher's own shutdown Event.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:650-700, 626-633` — _spawn_depth_republisher (C++ image_transport republish for /compressedDepth) is DEAD CODE — zero call sites (grep confirms only the def) — kept alongside the long inverse-depth-compression war-story comments.
    - CHANGE: Do not port. Delete in the replacement (and optionally from this file).
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:10-16 (docstring), 976` — Module docstring claims the publisher writes slot[(seq+1) % N]; the code writes slot[seq % N] (and the reader reads seq % N at live_shm_reader.py:390). Stale doc that could mislead the replacement author into an off-by-one slot scheme.
    - CHANGE: Implement seq % NUM_SLOTS with seq starting at 1 (reader treats 0 as no-frame, live_shm_reader.py:388-389); fix the docstring in the new module.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:448-460, 472` — Sticky intrinsics cache ~/.cache/dgs_camera_intrinsics.json written on every successful camera_info resolve — exists purely to survive Gazebo's lazy-publish failures.
    - CHANGE: Drop (ZED calibration is always available once the camera opens). Harmless to keep but it is dead weight and writes outside the project dir.

  _notes:_ VERDICT ON THE PLAN: keeping the SHM contract + reader untouched is FEASIBLE. Nothing in the SHM write path assumes ROS, 800x800, or square images (grep for "800" in the publisher returns zero data hits). The reader learns H/W/intrinsics from the JSON "ready" handshake and the six slot offsets from the header, so a ZED publisher that reuses _slot_layout/_write_header/_compute_header_field_offsets verbatim is automatically byte-compatible. The ONLY reader-side edit needed is the spawn wrapper (it hardcodes live_ros_publisher.py + the dynamic_gs_ros env + `source /opt/ros/noetic/setup.bash`).

================ BYTE-EXACT SHM SPEC (write a replacement from this) ================
SHM name: default "/dgs_live_shm" (publisher CLI --shm-name; reader passes same name). Publisher unlinks any stale region, then SharedMemory(create=True, size = 4096 + 4*slot_bytes) [live_ros_publisher.py:189-191,553-567]. Constants: NUM_SLOTS=4, HEADER_BYTES=4096 [190-191].

HEADER (bytes 0..4095; struct "<4sIIII4xdddd Q Q IIIIII II", 104 bytes used, rest padding) — packed ONCE at startup at TRUE struct offsets [240-257, call 570-576]:
  0  magic      4s  = b"DGS\0"
  4  version    u32 = 1
  8  height     u32      12 width u32      16 num_slots u32 = 4
  20 (4-byte pad)
  24 fx f64   32 fy f64   40 cx f64   48 cy f64
  56 latest_seq u64 = 0   64 slot_bytes u64
  72 rgb_off u32  76 depth_off u32  80 mask_off u32  84 pose_off u32  88 seq_off u32  92 stamp_off u32   (slot-relative)
  96 ready u32 = 1   100 shutdown u32 = 0
CRITICAL QUIRK (must replicate): runtime updates do NOT use these true offsets. _compute_header_field_offsets [publisher 263-301; identical copy in reader live_shm_reader.py:78-103] has an off-by-one-field prefix table starting at fx (entry ("fx","<4sIIII4x") omits fx's own d), so HDR_OFFSETS["latest_seq"]=48 (true 56) and HDR_OFFSETS["shutdown"]=96 (true 100). The publisher bumps the live seq counter at BYTE 48 via struct.pack_into("<Q", buf, 48, seq) [987-988], clobbering the packed cy double — harmless because the reader polls the same byte 48 [reader 387,404] and takes intrinsics from the ready JSON, never the header [reader 296-302]. shutdown is written at byte 96 [1285]; the reader never reads it (uses _decode_header only for magic + the six slot offsets [reader 320-327]). A replacement MUST copy _compute_header_field_offsets verbatim (or hardcode 48/96), NOT "fix" it — writing seq at true offset 56 breaks the reader silently (it would forever see latest_seq==initial bytes at 48 == packed cy).

SLOTS: slot i base = 4096 + i*slot_bytes. Layout from _slot_layout(H,W) [204-232], offsets relative to slot base:
  pose_off=0    : 16 x f64 LE, row-major 4x4 c2w
  seq_off=128   : u64
  stamp_off=136 : f64 seconds
  rgb_off=144   : H*W*3 u8, BGR interleaved, C row-major
  depth_off = 144+H*W*3 rounded up to 4-byte multiple : H*W f32 metres (invalid=0.0 by downstream convention)
  mask_off = depth_off + 4*H*W : H*W u8, 255=keep / 0=robot-excluded [convention documented 364-367; recorder save_data_img_depth_mask_pose.py:597-598]
  slot_bytes = end rounded up to multiple of 64
Computed: 800x800 -> rgb 144 / depth 1,920,144 / mask 4,480,144 / slot 5,120,192 (total shm 20,484,864 B). 1920x1200 -> depth 6,912,144 / mask 16,128,144 / slot 18,432,192 (total 73,732,864 B). 960x600 -> slot 4,608,192 (total 18,436,864 B). At 1920x1200@30Hz the publisher memcpys ~553 MB/s into shm — RAM-bandwidth trivial; the 4-slot reuse window stays 133 ms.

WRITE PROTOCOL per frame [967-988]: seq = ++counter (FIRST frame seq=1; reader treats latest_seq==0 as "no frame yet" [reader 388-389]); slot_idx = seq % 4 (module docstring line 14 saying "(seq+1) % N" is STALE — code is seq % N [976], reader agrees [reader 390]); write order: slot.seq, pose, stamp, rgb, depth, mask, THEN pack_into("<Q", buf, 48, seq). Reader does latest@48 -> copy slot -> re-check slot.seq and latest@48 [reader 386-406].

POSE SEMANTICS: 4x4 float64 c2w of the camera OPTICAL center in a metric world frame, OpenGL/nerfstudio camera axes (+x right, +y up, -z = view direction). Sim construction: Gazebo world pose of camera_pose_link (topic /dynaarm_arm/dynaarm_arm/camera1/gazebo_pose) @ URDF zero-config static offset camera_pose_link->camera_link_optical [_interpolate_c2w 746-783; _static_link_offset recorder 473-483], then rotate_camera_frame_only right-multiplies R by diag(1,-1,-1) (ROS-optical/OpenCV -> OpenGL) [recorder 84,140-144]. ZED positional tracking with COORDINATE_SYSTEM.RIGHT_HANDED_Y_UP + REFERENCE_FRAME.WORLD returns exactly this convention — use the 4x4 directly, float64, no flips. Sanity check after writing the new publisher: build_static_init_pointcloud back-projects with x=(u-cx)d/fx, y=-(v-cy)d/fy, z=-d then applies the stored c2w [1156-1162]; if the PLY overlays the real scene the convention is right.

DEPTH: float32 metres in SHM [985]; on disk uint16 mm TIFF [1084-1085]; invalid pixels MUST be 0.0 (downstream validity test is depth>0 [1129,1149]; recorder's own sanitizer maps ~isfinite|<=0 -> 0 [recorder 123-130]). RGB: BGR uint8 (sim path: cv2.imdecode IMREAD_COLOR [786-789]); ZED retrieve_image is BGRA -> drop alpha.

STAMP: f64 seconds, image timestamp; sim uses ROS sim-time epoch [938,970]. Use ZED image timestamp (get_timestamp(TIME_REFERENCE.IMAGE) ns * 1e-9). Must be monotonic; used for ordering/anchor identity, not synced against reader wall-clock in the publisher.

CONTROL CHANNEL: spawned with stdin/stdout pipes; stderr -> /tmp/dgs_live_publisher/publisher.stderr.log [reader 232-245]. Publisher dups fd1 to a private IPC writer and dup2's stderr onto fd1 so stray library prints cannot corrupt the JSON stream [63-67] — KEEP this (ZED SDK sdk_verbose prints to stdout). First line out: {"event":"ready", shm_name, width, height, fx, fy, cx, cy, num_slots, slot_bytes, header_bytes} [1368-1380] or {"event":"init_error", error} [1364]. Then line-delimited JSON request/response, no correlation IDs (reader is single-threaded against the pipe). Ops that MUST be implemented [1406-1472; reader send sites live_shm_reader.py:491-556]: wait_first_frame{timeout_s}->{ok}; capture_anchor{timeout_s}->{ok,seq,stamp}; start_recording{anchor_seq} (refuse with {"ok":false,"error":"anchor_not_latest"} if anchor_seq != latest); stop_recording->{ok,recorded} (quiesce in-flight disk writes first [1042-1066]); num_recorded; save_anchor_for_sam3{anchor_seq,debug_dir}->{ok,path}; save_anchor_depth_intrinsics{anchor_seq,artifact_dir}->{ok,depth_path,intrinsics_path}; build_init_pcd->{ok,ply_path}; pause_gazebo / unpause_gazebo (no-op on ZED, see sim_specific); shutdown (reply then exit). CLI: --live-root (required), --shm-name, --keyframe-translation-m=0.02, --keyframe-rotation-deg=20.0, --wipe-live-root, --record-replay [1341-1349].

DISK SIDE-DUTY (part of the contract — the ConcurrentFusionRunner tails transforms.json): while recording, each keyframe passing the 2cm/20deg OR-gate filter [309-347, 948-951] is written to <live_root>/static_scene/{rgb,depth,masks}/arm_%05d.{png,tiff,png} (BGR PNG, uint16-mm TIFF, mask PNG; prefix "arm" = IMAGE_NAME_PREFIX recorder:69) and transforms.json is atomically rewritten (tmp + os.replace) with {fl_x,fl_y,cx,cy,w,h,frames:[{file_path,depth_file_path,mask_path,transform_matrix}]} [1021-1106]. shutdown signal: byte 96 = 1 + SIGINT/SIGTERM/SIGHUP handlers calling shutdown then os._exit [1283-1287, 1393-1404].

================ DESIGN RISKS / OPEN QUESTIONS ================
1. WORLD-FRAME PERSISTENCE ACROSS PUBLISHER RESPAWNS is the biggest real-camera design risk (listed as blocker): bootstrap spawns TWO publishers, capture then teleop [867-870 comment]. Gazebo poses are absolute sim-world so respawn is free; ZED VIO re-zeroes its world at tracking start, so the teleop publisher's frames would be in a different frame than the static map (TSDF seed, trained splats). Options: keep ONE resident ZED publisher across both phases; or ZED area-memory file (save on stop, enable_area_memory+area_file_path on restart, wait for relocalization OK); or post-hoc ICP alignment against the TSDF seed. Also: ZED VIO drifts (cm-level over a session) where Gazebo FK was sub-mm + ICP-refined — invariants #2/#3 (camera-opt off, ICP-refined transforms.json) lean on near-perfect poses; the existing rewrite_transforms_with_icp.py / OnlineFusion ICP becomes load-bearing rather than cosmetic.
2. Stamp epoch changes from sim-time (~0-based) to UNIX epoch (~1.75e9). Nothing in the publisher cares; reader-side consumers of LiveFrame.stamp_sec were not audited here (out of my area) — verify none subtracts against time.time() expecting sim epoch.
3. Out of my area but triggered by this input change (flagged for the other reviewers): model background color (0.86,0.92,1.0) is baked in StaticGSModel/DynamicGSModel, not the publisher — nothing in the live publisher path uses the world-file background colors (_load_background_keep_mask lives in the recorder module [recorder 600-647] and is never called by the live publisher; its only mask source is _render_robot_exclusion_mask [960]). Downstream 16:10/1920x1200 handling (dataparser, gsplat render sizes, XFeat crop, SAM3D crops) also not assessed here — the SHM layer itself is resolution-agnostic.
4. ZED depth quality: sensor depth has edge bleeding/holes vs Gazebo's perfect depth; the TSDF (1.5 mm voxel) and the 3 mm proximity-cull floors were tuned on clean sim depth — expect retuning, and consider ZED confidence-map gating (write 0.0 where confidence is bad) since the pipeline already treats 0 as invalid.
5. Scale of work: ONE new module (zed_live_publisher.py) replacing live_ros_publisher.py; of its 1479 LOC, roughly 600-700 LOC (SHM layout/header/slot-views/write protocol, keyframe filter, JSON command loop, recording flow, PCD build, replay tap, shutdown plumbing) port verbatim; the replaced ingest (camera_info priming, subscribers/synchronizer, decoders, pose/joint interp, URDF mask) is ~450 LOC swapped for a ~100-150 LOC grab() loop. Plus a few-line spawn-wrapper edit in live_shm_reader.py and extraction of 5 ROS-free helpers from save_data_img_depth_mask_pose.py.

  **Reusable (works as-is):**
  - The SHM slot layout is fully parameterized on (height, width) — no 800x800 or square assumption anywhere in the write path; 1920x1200 and 960x600 work unchanged (computed slot_bytes 18,432,192 / 4,608,192; total region 73.7 MB / 18.4 MB)  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:204-232 (_slot_layout(height,width)), 554 (sized from intrinsics), 579-601 (slot views from intrinsics.height/width); grep for '800' in the file returns zero data hits)
  - Header pack/update machinery ports verbatim and stays reader-compatible, INCLUDING the latest_seq-at-byte-48 / shutdown-at-byte-96 offset quirk, because the reader carries an identical copy of the buggy offset table and polls the same bytes  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:263-301,987-988,1285 mirrored byte-for-byte by /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:78-103,387,404)
  - The cy header field being clobbered by seq writes is harmless with any camera: the reader takes intrinsics exclusively from the JSON ready event, and uses the decoded header only for magic validation + the six slot offsets  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:291-306 (intrinsics from ready JSON), 320-327 (_decode_header used only for magic + offsets))
  - The seq/slot write-read protocol (seq starts at 1, slot = seq % 4, slot.seq before payload, header bump last, reader retry on mismatch) is input-agnostic and needs zero change  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:967-988 + /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:373-414)
  - The JSON control protocol (fd-dup stdout protection, ready/init_error handshake, line-JSON op loop with all 11 ops) is camera-agnostic and ports verbatim; only pause/unpause bodies need stubbing  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:63-67,1327-1330,1368-1380,1406-1472)
  - The inlined ORB-SLAM-style keyframe filter (2 cm / 20 deg OR-gate) is numpy-only and pose-source-agnostic  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:309-347)
  - The disk recording flow — static_scene/{rgb,depth,masks}/arm_%05d, BGR PNG + uint16-mm TIFF + mask PNG, per-keyframe atomic transforms.json rewrite (tmp + os.replace) that the fusion watcher tails, stop_recording quiesce — works unchanged given sanitized depth  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:1021-1106 (NaN caveat at 1084 listed separately))
  - build_static_init_pointcloud's back-projection (x=(u-cx)d/fx, y=-(v-cy)d/fy, z=-d, then c2w) is consistent with the OpenGL c2w pose contract and validity convention depth>0, so it works with ZED data as-is  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:1149-1162)
  - save_anchor_for_sam3 / save_anchor_intrinsics_and_depth degrade gracefully with an all-white mask: the non-keep blackout becomes identity; shape-mismatch resize is already handled  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:1185-1196,1199-1211)
  - The replay-recording tap (--record-replay: fixed-size raw records + replay_meta.json + control-event log) is input-agnostic and worth porting for camera-free regression testing of the real-camera path  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:859-933,993-997,1251-1263)
  - The worker-thread + bounded drop-oldest queue architecture carries over (grab thread replaces the rospy callback as producer)  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:528-534,810-857)
  - Shutdown plumbing (atexit + SIGINT/SIGTERM/SIGHUP handlers, header shutdown byte, deliberate no-unlink/no-view-clear teardown rationale) is input-agnostic apart from the ROS unregister calls  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:1233-1287,1382-1404)
  - The mask convention (uint8, 255 = keep, 0 = excluded) and the depth-in-SHM unit (float32 metres) are explicit in the frame dataclass contract and stay valid for an all-white real-camera mask  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:355-367 (_StoredFrame docstring), mirrored at /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:125-140 (LiveFrame))
  - ZED with COORDINATE_SYSTEM.RIGHT_HANDED_Y_UP natively produces the exact pose convention the SHM contract carries (OpenGL c2w, metric world), so no conversion matrix is needed in the new publisher — the sim needed the diag(1,-1,-1) flip only because Gazebo/URDF poses are in ROS-optical axes  (/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_ros_publisher.py:783 (rotate_camera_frame_only application) + /home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/save_data_img_depth_mask_pose.py:84,140-144 (the OpenCV->OpenGL flip definition))

## AREA: reader/spawn (live_shm_reader.py)

- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:154-186, 277-284 (constructed at dynamic_gs_pipeline_live.py:133-140)` — LiveShmSubscriber.__init__ unconditionally spawns the ROS publisher: hardcoded conda env 'dynamic_gs_ros', '/opt/ros/noetic/setup.bash', live_ros_publisher.py script path, and '/home/mrc-cuhk/miniconda3' root; raises RuntimeError if the ROS env python or setup.bash is missing. So 'keep the reader untouched' is impossible as literally stated — the reader IS the publisher launcher.
    - CHANGE: Parameterize _spawn_publisher (e.g. env var DGS_LIVE_PUBLISHER=zed or a config field selecting publisher script + interpreter, no ROS sourcing for the ZED path). The new ZED publisher must honor the existing contract: (a) one JSON 'ready' line on stdout carrying width/height/fx/fy/cx/cy/num_slots/slot_bytes/header_bytes (reader:291-306), (b) the SHM layout — magic 'DGS\0', header fmt reader:57, per-slot BGR uint8 rgb + float32-m depth + uint8 keep-mask + float64 4x4 c2w + float64 stamp + uint64 seq with seqlock write order (reader:345-414), (c) the stdin 'shutdown' op (reader:567-575). Everything else about the SHM contract is publisher-agnostic. ~1 new publisher module + ~10-line change in _spawn_publisher.
- **[BLOCKER]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_pipeline_live.py:352-387 (also base:1491-1502, base:2733-2775)` — Every per-tick consumer assumes the live c2w is in the SAME world frame as the warm-cache Gaussians: D0 picks by distance from model.means (warm-cache world) to the SHM camera position; XFeat Kabsch back-projects via c2w into that world frame; the FF frustum cull/ICP uses scene_c2w against model.means. With URDF FK this frame was the fixed robot-base frame shared between static capture and dynamic phase. ZED VIO's world origin is wherever positional tracking started for THAT session — an unaligned VIO frame makes D0, tracking, CDN and FF all garbage. The per-FF-call ICP only absorbs ~2 cm (feedforward_anysplat_icp_max_dist_m=0.02, base:291).
    - CHANGE: Publisher/capture-side design, no pipeline code change: either (a) keep ONE continuous ZED positional-tracking session alive from static capture through teleop (publisher process survives the phase switch), (b) save/restore ZED area memory + relocalize at dynamic start, or (c) compute a one-time SE(3) alignment (e.g. ICP of the first live depth back-projection against depth_camera_init_points.ply) and premultiply it into every pose before the SHM write.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_pipeline_base.py:2355-2358 (also rgbd_decode.py:280, anysplat_decode.py:565,683, change_mask.py:190-197)` — Depth-validity gates assume sim-clean depth: every gate is '> 0' which rejects NaN and -Inf but PASSES +Inf (ZED TOO_FAR). In _feedforward_cull_in_front_of_depth, sensor_depth_at=+Inf makes in_front = depths_g < (Inf - tol) = True → mass-deletes scene/FF Gaussians under any TOO_FAR pixel inside the CDN region. rgbd_decode back-projects +Inf depth to infinity; anysplat ICP source and per-gauss depth lookup ('> 0.01') likewise pass +Inf; NaN poisons the masked avg-pool depth in change_mask downsampling (only consumed when change_mask_mode is a depth mode; default is 'rgb').
    - CHANGE: Sanitize at the publisher boundary so reader + pipeline stay byte-identical: the ZED publisher writes np.nan_to_num(depth, nan=0.0, posinf=0.0, neginf=0.0) before the SHM write. Belt-and-braces 1-liner alternative inside the pipeline: torch.nan_to_num at dynamic_gs_pipeline_live.py:568 in _batch_from_live_frame.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/anysplat_decode.py:626, 655-660 (called without override at dynamic_gs_pipeline_base.py:2794-2805)` — reproject_anysplat_to_scene defaults background_rgb=(0.86, 0.92, 1.0) — the Gazebo sky — and DROPS every AnySplat-predicted gaussian within ±0.08 of that color before insertion. _anysplat_bg_run never passes background_rgb, so the Gazebo filter runs on real-camera frames too: FF inserts on pale blue-white real surfaces (white walls, paper) get silently deleted.
    - CHANGE: Plumb a knob: pass background_tol=0.0 (disables the filter) or gate it on model.config.use_simulator_background at the call site in _anysplat_bg_run (dynamic_gs_pipeline_base.py:2794-2805). ~3 LOC.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/anysplat_decode.py:681, 703, 715-718 (vs third_party/AnySplat/src/utils/image.py:168-183)` — Square-resolution assumption in the AnySplat FF path (the DEFAULT mode, enable_feedforward_inpaint='anysplat_decode' at base:176): the worker preprocesses input images with aspect-preserving resize + CENTER-CROP to 448x448 (process_image), while reproject_anysplat_to_scene maps pred-pixels to the full frame via anisotropic STRETCH (cv2.resize of full sensor depth/CDN mask to 448x448, scene-K scaled by W_any/w, H_any/h). The two mappings coincide only for square input (sim 800x800, where the crop is a no-op). At 16:10 (1920x1200 → resize 716x448 → crop drops 134 px each side) the pixel correspondence breaks: FF anysplat inserts land at wrong world points.
    - CHANGE: Make the mappings agree: replicate process_image's resize+center-crop transform inside reproject_anysplat_to_scene for sensor depth, component mask and scene intrinsics (shift cx by the crop offset, scale by the aspect-preserving factor) — OR center-crop the live PNG dump in LiveDynamicGSPipeline._resolve_anysplat_context_image_paths (live:435-453) and crop depth/intrinsics consistently. Immediate zero-code workaround: run with enable_feedforward_inpaint='rgbd_decode', which is fully resolution/aspect-agnostic.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_model.py:60, 677-680 (consumed per tick at dynamic_gs_pipeline_live.py:528-531 and dynamic_gs_pipeline_base.py:2098-2101, 1793)` — Model render background is the baked Gazebo sky (use_simulator_background: bool = True → background_color (0.86,0.92,1.0)). With a real-world background, low/partial-alpha pixels composite sky-blue in the render while the live frame shows the real background → CDN RGB false positives at silhouettes and beyond the captured scene (the alpha coverage gate at change_mask.py:151-162 only drops pixels with alpha < 0.5, not partially-covered ones).
    - CHANGE: Launch real-camera runs with --pipeline.model.use_simulator_background False (flag already exists; falls back to Splatfacto's default background) — must be applied to BOTH static and dynamic model configs (invariant #6 in CLAUDE.md needs revisiting for real scenes). Zero new code.
- **[REQUIRED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_datamanager.py:118-121, 131-135, 31-35, 81` — Even the live pipeline constructs the full DynamicGSDataManager (via VanillaPipeline super().__init__, base:429-436): it hard-requires on-disk static_scene/ AND dynamic_scene/ folders, ≥1 dynamic train frame, depth_file_path for every dynamic frame (DynamicFrameDataset raises otherwise), and uint16-mm depth on disk (depth_unit_scale_factor=1e-3). Plus the warm cache static_scene/static_state.pt (base:469-491). Not sim logic per se, but the new ZED capture flow must reproduce this exact dataset layout or dynamic-gs-live won't construct.
    - CHANGE: The ZED capture/recording path must keep writing the existing layout: dynamic_scene/{rgb,depth,masks,transforms.json} with uint16-mm depth TIFFs and per-frame depth_file_path, all-white mask PNGs, OpenGL c2w in transforms.json metric units — and a ZED static-gs run must precede to produce static_state.pt. No datamanager code change needed.
- **[RECOMMENDED]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_pipeline_base.py:209 (and rgbd_decode.py:280-289)` — feedforward_rgbd_min_valid_fraction=0.95: rgbd FF skips any CDN component whose pixels are <95% depth-valid. Tuned for sim depth with zero holes; real ZED depth has NaN/0 holes at occlusion boundaries and dark/reflective surfaces, so many components will be silently skipped ('skipped valid_fraction=...').
    - CHANGE: Lower feedforward_rgbd_min_valid_fraction to ~0.5-0.7 for real depth (config-only), or change rgbd_decode to decode the valid subset instead of skipping the whole component.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_pipeline_live.py:249-261` — Sim-clock reset detection ('Gazebo's /clock was reset') re-arms the stamp dedup gate when stamp_sec jumps back >1 s. With monotonic ZED timestamps it never fires and is harmless — but it also usefully covers a publisher restart, so keep the gate.
    - CHANGE: Keep the logic; reword comment/log from Gazebo-specific to 'publisher clock reset/restart'. The new publisher must write monotonic stamp_sec (camera timestamp in seconds is fine).
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/dynamic_gs_pipeline_live.py:1-31, 85, 130-134 (also base:303-310, base:1900/1992-2000)` — Cosmetic ROS/gripper naming: module docstring 'Live ROS-fed', log line 'spawning ROS publisher + SHM subscriber', TrackerFrame doc 'ROS frame stamp', FF debug file '_gripper.png' and 'gripper_mask' variable names. No behavior coupling.
    - CHANGE: Rename/update docs when the ZED publisher lands ('live publisher', 'keep-mask'). Pure cleanup, ~20 lines of comments/strings.
- **[OPTIONAL]** `/home/mrc-cuhk/Documents/dynamic_gaussian_splat/scripts/dynamic_gs/utils/live_shm_reader.py:547-559` — pause_gazebo_physics/unpause_gazebo_physics control ops. NOT called anywhere in the live dynamic pipeline (only by the capture-stage live_session); both already swallow failures and return False.
    - CHANGE: ZED publisher may simply respond {ok: false} or omit the ops; later delete the methods. No pipeline change.

  _notes:_ Biggest design risk beyond the listed items: pose QUALITY, not convention. URDF FK + ICP-refined poses were sub-mm repeatable; ZED VIO drifts over minutes and jitters per frame. The CDN compares a render-from-pose against the live frame — camera pose error of more than ~1-2 cm reads as whole-scene 'change' (CDN noise → spurious FF calls), and XFeat's Kabsch folds camera-pose error directly into the object pose (world-frame correspondences). Existing mitigations: per-FF-call ICP refine absorbs <=2 cm (base:2767-2775), the coverage gate, and the depth_outlier CDN mode exists if needed. If drift shows up, consider having the ZED publisher fuse VIO with periodic ICP against the TSDF cloud. Other notes: (1) for dynamic-gs-live specifically the only control-pipe surface used is the ready event + 'shutdown' — capture_anchor/start_recording/build_init_pcd are live_session (capture-stage) concerns; (2) LIVE_ROOT defaults to a hardcoded path overridden by DGS_LIVE_ROOT (live_shm_reader.py:47-50) — the ZED publisher should accept --live-root identically; (3) the SHM mask plane must still be written (all 255) — the reader's slot layout requires it (reader:364-366); (4) perf: 1920x1200 is 3.6x the per-tick render pixels of 800x800 (CDN render + render_object_mask are full-res); prefer the 960x600 ZED mode first — change_mask_downsample_target_side=100 already keeps MS-SSIM cost constant; (5) change_mask_mode default is 'rgb' (dynamic_gs_model.py:77) so depth holes do NOT enter the CDN score, only the FF cull/decode paths — but if anyone flips to a depth mode, the NaN-in-avg-pool issue (change_mask.py:190-197) becomes live; (6) verification gap: I read the reader but NOT live_ros_publisher.py itself — the exact stamp source and pose frame written into SHM should be confirmed by whoever owns the publisher area; (7) interactive picker depends on SAM3/preseg artifacts on disk (load_sam3_objects, base:920-943) — falls back to the heuristic cleanly if a ZED capture lacks them, but the picker overlay then never appears.

  **Reusable (works as-is):**
  - All-white keep mask degrades safely to 'keep everything' at every consumer: _batch_from_live_frame turns it into an all-True bool mask; _build_tracking_rgb computes rgb*mask + bg*(1-mask) which is the identity for an all-ones mask (no background substitution happens); compute_change_mask's valid_mask becomes all-ones so nothing is excluded from CDN; erode of an all-ones keep mask is the identity (max_pool with zero padding on the inverted mask); XFeat's keep_region = gripper_keep ∩ dilated-object-mask reduces to the object-mask filter alone, with an existing fallback when too few points survive.  (dynamic_gs_pipeline_live.py:566-574; dynamic_gs_pipeline_base.py:1779-1794; change_detection/change_mask.py:133-149; utils/active_mask.py:43-52; utils/xfeat_motion.py:598-625; dynamic_gs_model.py:1813-1828)
  - No hardcoded resolution or aspect anywhere in the live frame path: the SHM slot geometry, intrinsics and per-frame Cameras are fully parameterized by the publisher's 'ready' handshake (width/height/fx/fy/cx/cy/num_slots/slot_bytes), so 1920x1200 or 960x600 16:10 frames flow through the reader and the per-tick pipeline without code change (square 800x800 is nowhere assumed).  (utils/live_shm_reader.py:296-306 (intrinsics from ready msg), 345-368 (slot views shaped H,W), 605-624 (cameras_from_live_frame); dynamic_gs_pipeline_live.py:141-145, 363-375 (W,H read from camera per tick); dynamic_gs_pipeline_base.py:1538-1554 (_crop_for_xfeat builds arbitrary-rect cameras), 2310-2326 (cull derives grid dims from the depth tensor and rescales intrinsics))
  - Per-tick camera convention is OpenGL c2w — exactly what ZED COORDINATE_SYSTEM.RIGHT_HANDED_Y_UP positional tracking produces. Every projection in the dynamic path uses depth = -z_cam and v = fy*(-y/d)+cy: D0 visibility projection, XFeat crop bbox, FF cull-in-front, the AnySplat frustum cull, ICP source back-projection and the final reprojection all share it. No URDF/FK math exists anywhere in the tick path; the c2w is consumed verbatim from SHM.  (dynamic_gs_pipeline_live.py:369-375; dynamic_gs_pipeline_base.py:1496-1502, 2333-2342, 2740-2749; utils/anysplat_decode.py:572-577 and 719-723 ('OpenGL convention' documented at 622); utils/live_shm_reader.py:612-622)
  - XFeat keypoint depth sampling already rejects NaN, ±Inf and 0 depth (isfinite AND > 0 on all four bilinear taps), so ZED invalid pixels cannot reach the Kabsch solver — the tracker core needs zero changes for real depth.  (utils/tracker_common.py:251-284 (sample_depth_bilinear); utils/xfeat_motion.py:504-517, 687-690 (all backprojection goes through it))
  - D0 object selection has no robot/gripper dependency: the live heuristic is a pure visibility gate (>=200 projected Gaussian centres in frustum) + closest-3D-centroid-to-camera ranking; d0_force_instance_id and the interactive viser picker (dropdown + Done, timeout fallback, mid-run 'Change object' reselect) are likewise camera-only. Nothing gripper-TCP-based exists in code (it is a roadmap item only).  (dynamic_gs_pipeline_live.py:328-400, 477-511; dynamic_gs_pipeline_base.py:170-173, 889-897 (_selection_fallback_id), 899-1112 (picker state machine))
  - No sim-time / RTF / robot-signal coupling in the tick or init flow: frame dedup is purely stamp-monotonicity, live mode ignores dynamic_steps_per_frame (SHM supply is the rate limiter), and startup waits only for the publisher ready event + first SHM frame (30 s timeout) — never on joint states, /clock or any teleop signal. D0 defers solely on object visibility; session ends via stdin 'stop' or signals.  (dynamic_gs_pipeline_live.py:246-262 (stamp dedup), 130-150 (ready + first frame waits), 486-496 (visibility-only defer), 212-233 (stdin watcher); dynamic_gs_pipeline_base.py:130-135 (live ignores pacing), 747-767 (get_train_loss_dict just ticks))
  - DynamicGSDataManager is pure dataset-layout code with zero ROS/Gazebo/sim references (grep for ros|gazebo|sim|urdf|joint|gripper|clock|catkin returns nothing); the static keyframe filter is pose-geometry only and the dataparser keeps metric, un-recentered poses (orientation/center none, auto_scale_poses=False) — all correct for ZED-captured datasets unchanged.  (dynamic_gs_datamanager.py:78-87 (dataparser config), 157-231 (keyframe filter); grep sweep over the file returned no sim references)
  - CDN is robust to the camera looking beyond the captured scene regardless of background, via the rendered-alpha coverage gate (pixels with accumulation < 0.5 are excluded from change detection) — this carries over to real scenes unchanged.  (change_detection/change_mask.py:151-162; alpha plumbed at dynamic_gs_pipeline_live.py:524, 548 and dynamic_gs_pipeline_base.py:1400-1405, 1424)
  - The lock-free SHM read protocol (seqlock retry, drop-oldest semantics), the model RLock concurrency design (tracker tick / FF bg insert / viser render exclusion), viser-direct visualization (camera feed thumbnail, follow-tracked-frame, picker GUI), the warm-cache load + _step_offset=10000 schedule bypass, the timing ledger, and the final-snapshot/timing-report atexit hooks are all input-source-agnostic and work unchanged.  (utils/live_shm_reader.py:373-414; dynamic_gs_pipeline_base.py:407-424 (locks+atexit), 469-537 (warm cache + step offset), 1118-1135 (_viser_lock_ctx), 1222-1254 (camera feed push))
  - The rgbd_decode FF mode (Mode B per-pixel back-projection) is fully resolution- and aspect-agnostic — depth grid dims are derived from the tensor and intrinsics rescaled to match — making it the safe FF mode for first real-ZED runs while the AnySplat aspect fix lands.  (dynamic_gs_pipeline_base.py:2153-2171 (decode call passes camera+depth straight through), 2310-2326 (grid-derived intrinsics rescale); utils/rgbd_decode.py:274-283 (mask & depth>0 validity, no fixed dims))

## AREA: reader/spawn (live_shm_reader.py)


  **Reusable (works as-is):**
